#separator:tab
#html:true
#notetype:Basic
#deck:Rust::Primitives
#tags column:3
Write 42 in all four bases Rust accepts. What is the rule for remembering the three prefixes?	<b><code>0x2A</code> hex &middot; <code>0o52</code> octal &middot; <code>0b10_1010</code> binary &middot; <code>42</code> decimal.</b><br><br>The rule: <b>the <code>0</code> means &lsquo;not decimal&rsquo;, and the letter after it is the first letter of the base&rsquo;s English name</b> &mdash; he<b>x</b>adecimal, <b>o</b>ctal, <b>b</b>inary.<br><br>There is no fourth prefix and no per-type variation, so that is the whole rule. Digit case does not matter (<code>0xff</code> == <code>0xFF</code>); the prefix letter is always lowercase.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {} {} {}&quot;, 0x2A, 0o52, 0b10_1010, 42);<br>}<br><br>// 42 42 42 42</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the same three prefixes, exactly &mdash; <code>0x2A</code>, <code>0o52</code>, <code>0b101010</code>. This one transfers with no adjustment at all.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives literals
What does this print? (Think before you answer &mdash; the C habit is a trap here.)<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {}&quot;, 0755, 0o755);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">755 493</pre><br><b>A bare leading zero means nothing in Rust.</b> <code>0755</code> is seven hundred fifty-five, decimal. Only <code>0o</code> asks for base 8.<br><br>In C, <code>0755</code> <i>is</i> octal &mdash; the bare leading zero is the prefix &mdash; which has produced a long tail of bugs where a zero-padded decimal quietly became a different number. Rust never adopted the form, so leading zeros are inert padding.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> made the same call, and its error message names the fix: <code>SyntaxError: leading zeros in decimal integer literals are not permitted; use an 0o prefix for octal integers</code>. Python 2 <i>did</i> read <code>0755</code> as octal; Python 3 removed it.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives literals gotcha
Three ways to say &ldquo;this 57 is a <code>u8</code>&rdquo;. Write them.	<b><code>57u8</code> &middot; <code>57_u8</code> &middot; <code>let c: u8 = 57;</code></b><br><br>All three are one instruction to the compiler, said in three places. The underscore before the suffix is optional and purely cosmetic.<br><br>Reach for the <b>suffix</b> when the value goes straight to something (<code>vec![0u8; 4]</code>, <code>x as f32 * 2.0_f32</code>) and the <b>annotation</b> when the value is being named.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a = 57u8;<br>    let b = 57_u8;<br>    let c: u8 = 57;<br>    println!(&quot;{}&quot;, a == b &amp;&amp; b == c);<br>}<br><br>// true</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> no suffix exists, because there is one <code>int</code>. Choosing a width is a decision Rust asks you to make and Python never does.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives literals
What is the underscore doing in <code>1_000_000</code>, and where may it go?	<b>Nothing, and anywhere.</b> It is a digit separator for the reader; the compiler ignores it entirely, in every base and any number of times.<br><br>Group at the boundary that matters: thousands in a decimal quantity, <b>bytes</b> in hex (<code>0xDEAD_BEEF</code>), <b>nibbles</b> in binary (<code>0b1011_1110</code>). <code>1_0_0</code> is legal, and nobody should.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {} {}&quot;, 1_000_000, 0xDEAD_BEEFu32, 1_0_0);<br>}<br><br>// 1000000 3735928559 100</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>1_000_000</code>, identical, since 3.6.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives literals
What does <code>b'A'</code> mean in Rust &mdash; and is it the same as Python&rsquo;s <code>b'A'</code>?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let byte = b&#x27;A&#x27;;<br>    let bytes = b&quot;Hi&quot;;<br>    println!(&quot;{} {:?} {}&quot;, byte, bytes, b&#x27;A&#x27; + 2);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">65 [72, 105] 67</pre><br><b>In Rust <code>b'A'</code> IS the number 65 &mdash; a <code>u8</code>, no container.</b> You can do arithmetic on it directly. <code>b"Hi"</code> is <code>&amp;[u8; 2]</code>.<br><br>The character between the quotes must be one ASCII byte: <code>b'&eacute;'</code> does not compile, because &eacute; is two bytes in UTF-8.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python &mdash; same spelling, different thing.</b> Python&rsquo;s <code>b'A'</code> is a <code>bytes</code> object of <b>length 1</b> &mdash; a container &mdash; so you need <code>b'A'[0]</code> to get 65.<br>&bull; Rust <code>b'A'</code> &harr; Python <code>b'A'[0]</code> or <code>ord('A')</code><br>&bull; Rust <code>b"Hi"</code> &harr; Python <code>b'Hi'</code><br>The familiar syntax maps to the <b>plural</b> form, which is the direction that produces a confusing type error rather than a wrong number.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives literals python-trap
&ldquo;<code>u8</code> is the only integer type suitable for a byte.&rdquo; True? Say what is actually going on.	<b>True narrowly, misleading broadly.</b> Nothing stops you storing 65 in an <code>i32</code>.<br><br>What is true: <b><code>u8</code> is the only type whose range is <i>exactly</i> a byte&rsquo;s</b> &mdash; <code>0..=255</code>, no value spare and none missing. So it is what the language and std use wherever the thing genuinely <i>is</i> a byte:<br>&bull; <code>b'A'</code> produces one<br>&bull; <code>b"Hi"</code> is an array of them<br>&bull; <code>String::as_bytes()</code> returns <code>&amp;[u8]</code><br>&bull; <code>File::read</code> fills a <code>&amp;mut [u8]</code><br><br>Picking <code>i32</code> there is picking a type that can hold <code>-1</code> and <code>300</code>, neither of which any byte is.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/meet_the_byte/index.html">meet_the_byte</a></div>	rust primitives integers
What range can a signed <code>iN</code> hold, as a formula in N? And <code>uN</code>?	<b>signed <code>iN</code>: &minus;2<sup>N&minus;1</sup> ..= 2<sup>N&minus;1</sup> &minus; 1</b><br><b>unsigned <code>uN</code>: 0 ..= 2<sup>N</sup> &minus; 1</b><br><br>So <code>i8</code> is &minus;128..=127 and <code>u8</code> is 0..=255.<br><br>The shape comes from two&rsquo;s complement: the top bit of a signed integer carries the sign, which halves the magnitude and shifts the range down by one &mdash; so there is exactly <b>one more value below zero than above it</b>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {} {} {}&quot;, i8::MIN, i8::MAX, u8::MIN, u8::MAX);<br>}<br><br>// -128 127 0 255</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> no formula to learn &mdash; <code>int</code> is arbitrary precision, so <code>2 ** 200</code> is exact. Every width here is a constraint Python does not have.<br><b>ABAP:</b> <code>b</code>/<code>s</code>/<code>i</code>/<code>int8</code> are 1/2/4/8 bytes, all <b>signed</b> &mdash; ABAP has no unsigned integer at all.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives integers
What does this print? (The answer is the sharpest consequence of two&rsquo;s complement.)<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{:?} {:?}&quot;, i8::MIN.checked_neg(), i8::MIN.checked_abs());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">None None</pre><br><b><code>-128</code> has no positive counterpart in <code>i8</code>.</b> The signed range is asymmetric &mdash; one more value below zero than above &mdash; so negating or taking the absolute value of <code>MIN</code> overflows.<br><br>Which means <b><code>i8::MIN.abs()</code> panics in debug and returns <code>-128</code> in release</b> &mdash; an absolute value that is negative. That is why <code>checked_abs</code> exists.<br><br>Unsigned types have no such hole: 0 is its own negation and there is nothing below it.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives integers gotcha
<code>255u8 + 1</code>. What happens &mdash; and what is the <i>bad</i> part of the answer?	<b>A debug build panics (<code>attempt to add with overflow</code>); a release build wraps to 0.</b> Both are documented and deliberate, neither is undefined behaviour.<br><br>The bad part: <b>the two builds do not agree</b>, so an overflow bug can pass every test you run and still wrap in production.<br><br>The fix is to stop using bare <code>+</code> where the width is in question and say which you meant:<br>&bull; <code>wrapping_add(1)</code> &rarr; 0 &mdash; modular, on purpose<br>&bull; <code>checked_add(1)</code> &rarr; <code>None</code> &mdash; the Option makes you handle it<br>&bull; <code>saturating_add(1)</code> &rarr; 255 &mdash; clamp at the ceiling<br>&bull; <code>overflowing_add(1)</code> &rarr; <code>(0, true)</code> &mdash; the value and whether it wrapped<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let m = u8::MAX;<br>    println!(&quot;{} {:?} {} {:?}&quot;,<br>        m.wrapping_add(1), m.checked_add(1), m.saturating_add(1), m.overflowing_add(1));<br>}<br><br>// 0 None 255 (0, true)</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> cannot happen on an <code>int</code>, but <i>does</i> happen the moment you opt into a fixed width elsewhere &mdash; <code>numpy.uint8(255) + 1</code> is 0, as are <code>struct</code>, <code>array</code>, <code>ctypes</code> and every integer column in your database.<br><b>ABAP:</b> raises <code>CX_SY_ARITHMETIC_OVERFLOW</code>. Three languages, three different choices.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/meet_the_byte/index.html">meet_the_byte</a></div>	rust primitives integers gotcha
<code>let n = 100_000u16;</code> &mdash; does that compile? Quote what rustc says.	<b>No. The LITERAL is rejected, before any arithmetic happens:</b><br><br><code>error: literal out of range for `u16`</code><br><code>= note: the literal `100_000u16` does not fit into the type `u16` whose range is `0..=65535`</code><br><code>= note: `#[deny(overflowing_literals)]` on by default</code><br><br>Note there is no <code>E0000</code> number: this is a <b>deny-by-default lint</b>, not a type error, which is why the message reads differently from the ones you are used to. The note prints the range, so the compiler tells you the answer to &lsquo;what does <code>u16</code> hold&rsquo; at the moment you need it.<br><br>Contrast with arithmetic: <code>65535u16 + 1</code> compiles fine and is a <i>run-time</i> question &mdash; panic in debug, wrap in release.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives integers compile-error
When do you reach for <code>usize</code> rather than <code>u32</code>? Give the rule and an example.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let names = [&quot;Ada&quot;, &quot;Ben&quot;, &quot;Cara&quot;];<br>    let n: usize = names.len();<br>    let i: usize = 1;<br>    println!(&quot;{} of {n}&quot;, names[i]);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">Ben of 3</pre><br><b>A <i>quantity</i> is a <code>u32</code>; a <i>position</i> in a collection is a <code>usize</code>.</b><br><br><code>usize</code> is the machine&rsquo;s pointer width &mdash; 8 bytes on a 64-bit target, 4 on a 32-bit one &mdash; and its job is <b>positions in memory</b>. Every length, index, capacity and byte count in the standard library is one, which is why <code>.len()</code> hands you a <code>usize</code> and why indexing demands one.<br><br>So a vote total is <code>u32</code>; the position of a candidate in a slice is <code>usize</code>. That is also where <code>scores[i as usize]</code> comes from, when <code>i</code> arrived as a <code>u32</code>.<br><br><code>isize</code> is rare: pointer offsets and FFI.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> not exposed &mdash; you index with any <code>int</code>, and a negative one counts from the end. Rust has no negative indexing at all, which is half the reason the index type is unsigned.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/values/index.html">values</a></div>	rust primitives integers
How do you write an <b>unsigned</b> floating-point literal in Rust?	<b>You cannot. There is no <code>uf32</code> or <code>uf64</code>, and there cannot be.</b><br><br>IEEE 754 puts a sign bit at the top of every float, so signedness is part of the <i>format</i>, not a choice the type makes. The bit is there even at zero, which is why floats have two zeros that compare equal:<br><br>If you need &ldquo;a float that cannot be negative&rdquo;, that is a newtype with a checked constructor, not a primitive.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {}&quot;, -0.0 == 0.0, (-0.0f64).is_sign_negative());<br>}<br><br>// true true</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives floats
What is the default float type, why, and what does &ldquo;double precision&rdquo; actually count?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {} {} {}&quot;,<br>        f32::MANTISSA_DIGITS, f32::DIGITS, f64::MANTISSA_DIGITS, f64::DIGITS);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">24 6 53 15</pre><br><b><code>f64</code> is the default</b> &mdash; roughly as fast as <code>f32</code> on any machine you will meet, and it remembers more than twice as much.<br><br><b>Precision counts significant BITS, not decimal places:</b><br>&bull; <code>f32</code> = IEEE 754 binary32 = <b>single</b> precision: 24 significand bits, ~6 reliable decimal digits<br>&bull; <code>f64</code> = IEEE 754 binary64 = <b>double</b> precision: 53 significand bits, ~15 digits<br><br>&ldquo;Double&rdquo; is a historical name, from when the narrow one was the machine&rsquo;s native float and the wide one took two registers.<br><br>More bits is not <i>exact</i>: <code>0.1</code> is representable in neither. <code>f64</code> gets the same wrong answer 29 bits further right.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>float</code> is always a C double &mdash; i.e. always <code>f64</code>. There is no single-precision float in the language, so <code>f32</code> is a choice Rust offers that Python does not.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/what_a_float_stores/index.html">what_a_float_stores</a></div>	rust primitives floats
Does this compile?<br><br>If not, what did rustc think you wrote?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let f = 2.f32;<br>    println!(&quot;{f}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0610]  -- it does not compile</pre><br><b>No &mdash; <code>E0610</code>. rustc parsed <code>2.f32</code> as <i>field <code>f32</code> of the number 2</i>,</b> and primitives have no fields.<br><br>Its help line gives the fix: <i>&ldquo;if intended to be a floating point literal, consider adding a <code>0</code> after the period&rdquo;</i>.<br><br>Write <code>2.0f32</code> or <code>2_f32</code>. Note what <i>does</i> work:<br>&bull; <code>2.0</code> &rarr; <code>f64</code><br>&bull; <code>2.</code> &rarr; <code>f64</code> (legal! plain <code>2</code> would be an integer)<br>&bull; <code>1e6</code> &rarr; <code>f64</code><br>&bull; <code>2_f32</code> &rarr; no dot needed once the suffix says float<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/19_Numbers/writing_a_number_down/index.html">writing_a_number_down</a></div>	rust primitives floats compile-error
How big is a <code>bool</code>, and how big is <code>Option&lt;bool&gt;</code>?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {} {}&quot;,<br>        size_of::&lt;bool&gt;(), size_of::&lt;Option&lt;bool&gt;&gt;(), size_of::&lt;[bool; 8]&gt;());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">1 1 8</pre><br><b>One byte &mdash; one bit of information in one byte of space</b>, because a byte is the smallest thing a machine can address. Eight bools in an array are eight BYTES.<br><br><code>Option&lt;bool&gt;</code> is <b>also</b> one byte: a <code>bool</code> uses two of its 256 bit patterns, so <code>None</code> is stored <i>in</i> the value rather than beside it. That is the niche optimization, and <code>bool</code> is its clearest case.<br><br>Packing eight bools into one byte is bit flags, and it is deliberate work you opt into.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/meet_the_bool/index.html">meet_the_bool</a></div>	rust primitives bool
Does this compile?<br><br>Name the rule, and where else it applies.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let n = 1;<br>    if n {<br>        println!(&quot;yes&quot;);<br>    }<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0308]  -- it does not compile</pre><br><b>No &mdash; <code>E0308</code>: expected <code>bool</code>, found integer. There is no truthiness anywhere in Rust.</b><br><br>Not just <code>if</code>: the same rule holds in <code>while</code>, <code>match</code> guards, <code>filter</code> and <code>assert!</code>, because nothing in the language converts a value to a truth.<br><br>A <code>Vec</code> is not false when empty; an <code>Option</code> is not false when <code>None</code>; <code>0.0</code> is not false. Each has its own question &mdash; <code>is_empty()</code>, <code>is_none()</code>, <code>!= 0.0</code> &mdash; and you pick the one you meant.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> truthiness everywhere &mdash; <code>if []</code>, <code>if ""</code>, <code>if 0</code>, <code>if None</code> are all false, and <code>__bool__</code> lets any class join in. The habit that transfers badly is <code>if my_list:</code> &rarr; write <code>if !v.is_empty()</code>.<br><b>ABAP:</b> no boolean type at all &mdash; <code>abap_bool</code> is a char with <code>abap_true = 'X'</code>, so <code>IF lv_flag = abap_true</code> is a char comparison. Rust&rsquo;s <code>if flag</code> is the condition itself.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/meet_the_bool/index.html">meet_the_bool</a></div>	rust primitives bool python-trap
Python counts <code>True</code>s with <code>sum(flags)</code>. What is the Rust line &mdash; and why is it not a sum?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let flags = [true, false, true, true, false];<br>    println!(&quot;{}&quot;, flags.iter().filter(|&amp;&amp;b| b).count());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">3</pre><br><b><code>bool</code> is not a number, so counting is a <i>count</i>, not a sum.</b> <code>true + 1</code> is <code>error[E0369]: cannot add {integer} to bool</code>.<br><br><code>flags.iter().map(|&amp;b| b as u32).sum()</code> gives the same 3, but the reader has to work out from a cast that counting was the point. Prefer <code>filter(..).count()</code>.<br><br>Both directions need saying out loud:<br>&bull; <code>bool</code> &rarr; number: <code>b as u8</code> or <code>u8::from(b)</code> &mdash; <code>true</code> is 1, guaranteed<br>&bull; number &rarr; <code>bool</code>: <code>n != 0</code>. <code>1u8 as bool</code> is <code>E0054</code>, and rustc&rsquo;s help says <i>&ldquo;compare with zero instead&rdquo;</i><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>bool</code> is a <b>subclass of <code>int</code></b>, so <code>True + 1 == 2</code> and <code>[False, True][flag]</code> indexes a list with a boolean. None of that has a Rust translation.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/meet_the_bool/index.html">meet_the_bool</a></div>	rust primitives bool python-trap
<code>&amp;&amp;</code> and <code>&amp;</code> both compile on bools and return the same value. What is the difference, and when does it bite?	<b><code>&amp;&amp;</code> short-circuits; <code>&amp;</code> always evaluates both sides.</b> Nothing warns you, because both type-check as <code>bool &rarr; bool &rarr; bool</code>.<br><br>It bites hardest on a <b>guard</b>, where the left half exists to make the right half safe to ask:<br><br><code>(i &lt; v.len()) &amp;&amp; (v[i] &gt; 0)</code> &mdash; safe, returns false<br><code>(i &lt; v.len()) &amp; (v[i] &gt; 0)</code> &mdash; <b>panics</b>, index out of bounds<br><br>Same shape with a side effect instead of a panic: <code>cache_ok &amp; fetch_from_network()</code> does the fetch every time. Use <code>&amp;</code>/<code>|</code> on bools only when you want both sides evaluated on purpose &mdash; rare enough to deserve a comment.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![5u8, 3, 0];<br>    let i = 7;<br>    println!(&quot;{}&quot;, (i &lt; v.len()) &amp;&amp; (v[i] &gt; 0));<br>}<br><br>// false</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the same pair &mdash; <code>and</code>/<code>or</code> short-circuit, <code>&amp;</code>/<code>|</code> on bools do not. Same trap, though Rust gives you more ways to feel it, since the right side may panic rather than merely be slow.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/meet_the_bool/index.html">meet_the_bool</a></div>	rust primitives bool gotcha
How many bytes is a <code>char</code>, and <b>why that number</b>?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{} {:?} {}&quot;, size_of::&lt;char&gt;(), char::MAX, char::MAX as u32);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">4 &#x27;\u{10ffff}&#x27; 1114111</pre><br><b>Four. Because a <code>char</code> is one Unicode scalar value, and the largest is U+10FFFF &mdash; which needs 21 bits.</b><br><br>21 bits does not fit in 8 or 16, so the next byte-sized width is 32.<br><br>It is fixed-width <i>because</i> it is <b>decoded</b>: you can compare it, classify it, and range over it (<code>'0'..='9'</code>). Inside a <code>String</code> the same character is <b>1 to 4 UTF-8 bytes</b>, because encoded bytes want to be small.<br><br>So <code>'A'</code> costs 4 bytes as a value and 1 byte inside a string.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>C:</b> <code>char</code> <i>is</i> the byte &mdash; the same word for a different object, which is the single most common confusion coming from C.<br><b>Python:</b> no character type at all; a 1-length <code>str</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/14_Strings/meet_the_char/index.html">meet_the_char</a></div>	rust primitives char unicode
Rust has four literal forms that differ only in quotes and a prefix. Name all four and their types.	<b><code>'A'</code> &rarr; <code>char</code></b> (4 bytes, one Unicode scalar value)<br><b><code>"A"</code> &rarr; <code>&amp;str</code></b> (UTF-8 bytes)<br><b><code>b'A'</code> &rarr; <code>u8</code></b> (the number 65)<br><b><code>b"A"</code> &rarr; <code>&amp;[u8; 1]</code></b><br><br>Single quotes are a character, double quotes are a string, and a leading <code>b</code> means bytes rather than text. Two of the four print the same and are not the same type.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let c: char = &#x27;A&#x27;;<br>    let s: &amp;str = &quot;A&quot;;<br>    let b: u8 = b&#x27;A&#x27;;<br>    let bs: &amp;[u8; 1] = b&quot;A&quot;;<br>    println!(&quot;{} {} {} {:?}&quot;, c, s, b, bs);<br>}<br><br>// A A 65 [65]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>'A'</code> and <code>"A"</code> are the <i>same thing</i> &mdash; quote style is pure preference. In Rust they are different types, which is the first thing to unlearn.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/14_Strings/meet_the_char/index.html">meet_the_char</a></div>	rust primitives char literals python-trap
Does this compile?<br><br>What is the rule about a tuple&rsquo;s type?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let t: (u8, char, bool) = (5, &#x27;A&#x27;, true);<br>    let (a, b) = t;<br>    println!(&quot;{a} {b}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0308]  -- it does not compile</pre><br><b>No. A tuple&rsquo;s <i>length and element types ARE its type</i></b>, so a three-element tuple cannot be destructured into two names.<br><br><code>(u8, char, bool)</code> and <code>(u8, bool, char)</code> are different types, and neither is <code>(u8, char)</code>.<br><br>Getting values out, two ways:<br>&bull; destructure: <code>let (a, b, c) = t;</code><br>&bull; index by position: <code>t.0</code>, <code>t.1</code>, <code>t.2</code><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>a, b = (1, 2, 3)</code> raises <code>ValueError: too many values to unpack</code> &mdash; <b>at run time</b>. Rust moves the same error to compile time. Also missing in Rust: <code>*rest</code> unpacking and <code>t + (1,)</code>. What <i>does</i> transfer exactly: the comma rule, <code>(7)</code> is 7 and <code>(7,)</code> is a one-tuple.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/tuples/index.html">tuples</a></div>	rust primitives tuples compile-error python-trap
What is <code>()</code> called, how many values does it have, and how big is it?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{:?} {} {}&quot;, (), size_of::&lt;()&gt;(), size_of::&lt;[(); 1000]&gt;());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">() 0 0</pre><br><b>The <i>unit type</i> &mdash; the empty tuple. Exactly ONE value, and ZERO bytes.</b><br><br>A type with one value carries no information, so there is nothing to store; a thousand of them still occupy nothing. It is both the type and its single value, written the same way.<br><br>Counting values orders the primitives: <code>u8</code> has 256, <code>bool</code> has 2, <code>()</code> has 1, and <code>!</code> (never) has 0.<br><br>Where it comes from: <b>a function with no <code>-&gt;</code></b> returns it, and <b>a semicolon</b> produces it &mdash; <code>{ 7; }</code> is <code>()</code> while <code>{ 7 }</code> is 7. That one rule is behind every <i>&ldquo;expected <code>i32</code>, found <code>()</code>&rdquo;</i>.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the nearest thing is <code>None</code>, and the resemblance misleads. Python&rsquo;s <code>None</code> does two jobs &mdash; &ldquo;no return value&rdquo; <i>and</i> &ldquo;missing&rdquo;. Rust splits them: <code>()</code> is &ldquo;done, nothing to report&rdquo;, <code>Option::None</code> is &ldquo;there is no value here&rdquo;.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/the_unit_type/index.html">the_unit_type</a></div>	rust primitives unit tuples
What does this print? (Then say what the second value is, and why.)<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![3u8, 1, 5];<br>    let x = v.sort();<br>    println!(&quot;{v:?} {x:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[1, 3, 5] ()</pre><br><b><code>x</code> is <code>()</code> &mdash; you took the receipt instead of the result.</b> Every in-place method returns unit, because the answer was written back into the receiver.<br><br>Nothing complains until you use <code>x</code>, and then the message names the type outright: <code>error[E0599]: no method named 'len' found for unit type '()'</code>.<br><br><code>push</code>, <code>dedup</code>, <code>retain</code>, <code>clear</code> and <code>sort_unstable</code> all do this &mdash; which is also why none of them chains: <code>v.push(9).dedup()</code> does not compile. <b>Mutate, then use</b>; or clone first and sort the copy.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the identical bug &mdash; <code>x = lst.sort()</code> gives <code>None</code>, and you find out at run time with <code>AttributeError: 'NoneType' object has no attribute ...</code>. Both languages made <code>sort</code> return nothing <i>deliberately</i>, so a mutation cannot be mistaken for a copy.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/the_unit_type/index.html">the_unit_type</a></div>	rust primitives unit gotcha python-trap
What does <code>Result&lt;(), String&gt;</code> describe, and how do you write its success value?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    fn check(score: u8) -&gt; Result&lt;(), String&gt; {<br>        if score &lt;= 5 { Ok(()) } else { Err(format!(&quot;{score} out of range&quot;)) }<br>    }<br>    println!(&quot;{:?} {:?}&quot;, check(5), check(9));<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">Ok(()) Err(&quot;9 out of range&quot;)</pre><br><b>A job that either works with no payload, or fails with a reason. The success value is <code>Ok(())</code>.</b><br><br>It reads oddly the first time and says something precise: <i>it worked, and there is nothing to hand back</i>. A validator, a <code>write!</code>, a <code>File::set_len</code> &mdash; all this shape.<br><br><code>?</code> works as it always does; on success it unwraps a value that carries nothing, so nothing is lost.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> this is <code>sy-subrc</code> with the discipline added. Same idea &mdash; success carries no payload, failure carries a reason &mdash; but <code>sy-subrc</code> can be ignored by not looking at it, while <code>Result</code> is <code>#[must_use]</code> and the compiler warns when you drop it.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/the_unit_type/index.html">the_unit_type</a></div>	rust primitives unit result
Two ways to initialise an array. Write both, and say what is in the type.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a: [i32; 5] = [1, 2, 3, 4, 5];<br>    let b = [3; 5];<br>    println!(&quot;{a:?} {b:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[1, 2, 3, 4, 5] [3, 3, 3, 3, 3]</pre><br><b>Element by element, <code>[1, 2, 3, 4, 5]</code>; or all at once, <code>[3; 5]</code> &mdash; the value, a semicolon, the count.</b><br><br><b>The length is part of the type.</b> <code>[i32; 5]</code> and <code>[i32; 3]</code> are as different as <code>u32</code> and <code>String</code>, so a function taking one will not accept the other.<br><br>Arrays are fixed length, always. The elements sit inline &mdash; <code>size_of::&lt;[u32; 5]&gt;()</code> is 20, five values and no header.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>[3] * 5</code> is the counterpart of <code>[3; 5]</code>, and the famous Python trap &mdash; <code>[[0] * 3] * 2</code> aliases the inner list &mdash; has no Rust equivalent here, because <code>[[0; 3]; 2]</code> copies.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/array_or_vec/index.html">array_or_vec</a></div>	rust primitives arrays
Does this compile?<br><br>State the rule in one sentence.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let ballots = [&quot;a&quot;, &quot;b&quot;, &quot;c&quot;];<br>    let n = ballots.len();<br>    let counts = [0u32; n];<br>    println!(&quot;{counts:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0435]  -- it does not compile</pre><br><b>No &mdash; <code>E0435</code>: attempt to use a non-constant value in a constant. An array length is a CONSTANT, full stop.</b><br><br><code>n</code> came from a value, so <code>vec![0u32; n]</code> is not a fallback &mdash; it is the only thing that expresses what you meant.<br><br>This is the real reason the default is <code>Vec</code>: almost every length in a real program comes from input, and input length is not knowable when you compile.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/array_or_vec/index.html">array_or_vec</a></div>	rust primitives arrays compile-error
Array or <code>Vec</code>? Give the default and the four things an array actually buys.	<b>Default to <code>Vec</code></b> &mdash; because almost every length comes from input, and input length is not knowable at compile time.<br><br><b>An array wins when the length is a fact about the PROBLEM:</b><br>1. <b>The wrong length is a compile error.</b> A fn taking <code>[u8; 32]</code> cannot be handed 31 bytes; the <code>Vec</code> version compiles and produces a wrong answer.<br>2. <b>An array of <code>Copy</code> is <code>Copy</code>.</b> <code>let snap = arr;</code> is a real copy, no <code>.clone()</code>, and the original stays usable.<br>3. <b>It works in a <code>const</code>.</b> <code>const W: [u32; 6] = [..]</code>; a <code>Vec</code> cannot, since there is no allocator at compile time.<br>4. <b>No allocator at all</b> &mdash; which is why <code>no_std</code> and embedded code use arrays.<br><br>And the question mostly dissolves: take <code>&amp;[T]</code> in the signature and both callers work, so this is a <i>storage</i> decision, not an API one.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    fn total(counts: &amp;[u32]) -&gt; u32 { counts.iter().sum() }<br>    let fixed = [4u32, 0, 2];<br>    let grown = vec![4u32, 0, 2];<br>    println!(&quot;{} {}&quot;, total(&amp;fixed), total(&amp;grown));<br>}<br><br>// 6 6</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> nothing to choose &mdash; <code>list</code> is <code>Vec</code>, and there is no array. The habit that transfers badly is reaching for the list every time, which in Rust means a heap allocation for a three-element lookup table that could have been a <code>const</code>.<br><b>ABAP:</b> <code>Vec&lt;T&gt;</code> is a <code>STANDARD TABLE OF</code>; an array is closer to a fixed-length structure. But <code>SORTED</code>/<code>HASHED</code> tables are <code>BTreeMap</code>/<code>HashMap</code> in Rust, not flavours of <code>Vec</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/array_or_vec/index.html">array_or_vec</a></div>	rust primitives arrays vec
What are <code>x</code> and <code>y</code>? Explain <b>both</b> reasons.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let x = 10;<br>    let y = {<br>        let x = 3;<br>        x + 1<br>    };<br>    println!(&quot;{x} {y}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">10 4</pre><br><b><code>x</code> is 10 and <code>y</code> is 4, for two independent reasons.</b><br><br>1. <b>The block is an expression.</b> Its value is its last expression <i>without</i> a semicolon &mdash; <code>x + 1</code> has no <code>;</code>, so the block evaluates to 4.<br>2. <b>The inner <code>let x = 3</code> shadows</b> the outer <code>x</code> only inside the braces, so the outer one is still 10.<br><br>The <code>;</code> after the closing <code>}</code> ends the <code>let</code> statement &mdash; required, and unrelated to either. Put a semicolon on <code>x + 1;</code> and the block&rsquo;s value becomes <code>()</code>.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> has neither &mdash; no block expressions, and no shadowing in an inner scope (a nested <code>x = 3</code> in the same function rebinds the <i>same</i> name). The Rust habit this enables has no Python counterpart: <code>let config = { ...build it...  built };</code>, where the scratch variables die at the brace.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/a_block_is_an_expression/index.html">a_block_is_an_expression</a></div>	rust primitives expressions
Statement or expression &mdash; what is the difference, and which one is <code>let x = 5;</code>?	<b>An expression evaluates to a value; a statement performs an action and does not.</b> <code>let x = 5;</code> is a <b>statement</b>.<br><br>Which is why <code>let a = (let b = 5);</code> does not compile &mdash; there is no value to bind.<br><br>Rust is expression-heavy, so the things that are statements in other languages have values here:<br>&bull; <code>let size = if n &lt; 10 { "small" } else { "large" };</code><br>&bull; <code>let v = match c { .. };</code><br>&bull; <code>let v = loop { break 7; };</code><br>&bull; a function body&rsquo;s last expression is its return value, no <code>return</code> needed<br><br>A <code>;</code> turns an expression into a statement by discarding its value and leaving <code>()</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let n = 4;<br>    let size = if n &lt; 10 { &quot;small&quot; } else { &quot;large&quot; };<br>    let doubled = { n * 2 };<br>    println!(&quot;{size} {doubled}&quot;);<br>}<br><br>// small 8</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the conditional expression <code>"small" if n &lt; 10 else "large"</code> is the one counterpart; <code>match</code> and <code>loop</code> have none. Python&rsquo;s statement/expression line is drawn in a very different place, which is why <code>return</code> is mandatory there and optional here.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/15_First_Programs/a_block_is_an_expression/index.html">a_block_is_an_expression</a></div>	rust primitives expressions
Reverse an array. Write the two ways, and say what each returns.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a = [1, 2, 3];<br>    let backwards: Vec&lt;i32&gt; = a.iter().rev().copied().collect();<br>    let mut b = [1, 2, 3];<br>    b.reverse();<br>    println!(&quot;{backwards:?} {b:?} {a:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[3, 2, 1] [3, 2, 1] [1, 2, 3]</pre><br><b><code>a.iter().rev()</code></b> returns a <b>lazy iterator</b>, not a reversed array &mdash; nothing has moved, and the original is untouched. Add <code>.collect()</code> if you want a container.<br><br><b><code>b.reverse()</code></b> reverses <b>in place</b> and returns <code>()</code>, like every other in-place method.<br><br><code>.rev()</code> needs <code>DoubleEndedIterator</code>, which slice and array iterators are &mdash; but a <code>HashMap</code> iterator is not, so <code>.rev()</code> on one does not compile.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the same split, same names almost &mdash; <code>reversed(a)</code> is the lazy view (<code>list(...)</code> to materialise) and <code>a.reverse()</code> is in place and returns <code>None</code>. This one maps across cleanly.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/double_ended_and_exact_size/index.html">double_ended_and_exact_size</a></div>	rust primitives arrays iterators
