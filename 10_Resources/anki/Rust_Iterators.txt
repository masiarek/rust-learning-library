#separator:tab
#html:true
#notetype:Basic
#deck:Rust::Iterators
#tags column:3
What does this print, and in what ORDER?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    let doubled = v.iter().map(|x| { println!(&quot;seen {x}&quot;); x * 2 });<br>    println!(&quot;nothing yet&quot;);<br>    let out: Vec&lt;i32&gt; = doubled.collect();<br>    println!(&quot;{out:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">nothing yet<br>seen 1<br>seen 2<br>seen 3<br>[2, 4, 6]</pre><br><b>An adapter such as <code>map</code> or <code>filter</code> computes NOTHING. It builds a value describing the work. A <b>consumer</b> is what runs it.</b><br><br>Nothing inside the closure happens until <code>collect</code> asks &mdash; which is why <code>nothing yet</code> prints first.<br><br>Consumers: <code>collect</code>, <code>sum</code>, <code>count</code>, <code>fold</code>, <code>for_each</code>, <code>find</code>, <code>any</code>, <code>all</code>, <code>max</code>, and a <code>for</code> loop.<br><br>An unconsumed chain is a value you built and dropped &mdash; <code>Iterator</code> is <code>#[must_use]</code>, so rustc warns.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the same &mdash; <code>map()</code> and generators are lazy in Python 3; <code>list()</code> is the consumer.<br><b>JavaScript:</b> NOT the same &mdash; <code>Array.prototype.map</code> is eager and allocates immediately.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/iterators_are_lazy/index.html">iterators_are_lazy</a></div>	rust iterators core
How do you tell an <b>adapter</b> from a <b>consumer</b> by looking at it?	<b>An adapter returns something that still implements <code>Iterator</code>. A consumer returns an answer.</b><br><br>You never have to memorise the list &mdash; read the return type. <code>map</code> &rarr; <code>Map&lt;...&gt;</code> (an iterator). <code>sum</code> &rarr; <code>i32</code> (an answer).<br><br>The practical consequence: <b>a chain of adapters costs nothing until a consumer is attached</b>, and adapters compose without allocating. <code>collect</code> is the only common consumer that allocates.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3, 4];<br>    // adapters: still lazy, no work done<br>    let chain = v.iter().map(|x| x * 2).filter(|x| x &gt; &amp;4);<br>    // one consumer runs the whole chain, once, allocating once<br>    println!(&quot;{:?}&quot;, chain.collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{}&quot;, v.iter().sum::&lt;i32&gt;());<br>    println!(&quot;{}&quot;, v.iter().count());<br>}<br><br>// [6, 8]<br>// 10<br>// 4</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/iterators_are_lazy/index.html">iterators_are_lazy</a></div>	rust iterators core
Why does driving an iterator by hand need <code>let mut</code>?	<b>Because <code>next(&amp;mut self)</code> CONSUMES an item &mdash; it changes the iterator's own state.</b><br><br><code>fn next(&amp;mut self) -&gt; Option&lt;Self::Item&gt;</code>. An iterator is a cursor, not a view: asking for the next item permanently advances it. <code>None</code> means exhausted.<br><br>This is also why an iterator cannot be read twice &mdash; and why <code>collect</code> exists.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2];<br>    let mut it = v.iter();<br>    println!(&quot;{:?} {:?} {:?}&quot;, it.next(), it.next(), it.next());<br>}<br><br>// Some(1) Some(2) None</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/implementing_iterator/index.html">implementing_iterator</a></div>	rust iterators core
What does <code>for x in thing { ... }</code> desugar to?	<b><code>IntoIterator::into_iter(thing)</code>, then <code>while let Some(x) = it.next()</code>.</b><br><br>So <code>for</code> works on anything implementing <code>IntoIterator</code> &mdash; and the <code>&amp;</code> you write picks the implementation:<br>• <code>for x in &amp;v</code> &rarr; <code>iter()</code>, x is <code>&amp;T</code><br>• <code>for x in &amp;mut v</code> &rarr; <code>iter_mut()</code>, x is <code>&amp;mut T</code><br>• <code>for x in v</code> &rarr; <code>into_iter()</code>, x is <code>T</code>, <b>v is consumed</b><br><br>There is no separate <code>.iter()</code> call in the desugaring &mdash; that is why <code>for x in v.iter()</code> and <code>for x in &amp;v</code> are the same loop.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    let mut it = (&amp;v).into_iter();<br>    while let Some(x) = it.next() { print!(&quot;{x} &quot;); }<br>    println!();<br>    for x in &amp;v { print!(&quot;{x} &quot;); }<br>    println!();<br>}<br><br>// 1 2 3 <br>// 1 2 3 </pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/iter_iter_mut_into_iter/index.html">iter_iter_mut_into_iter</a></div>	rust iterators core
<code>collect()</code> can build a Vec, a String, a HashMap, a HashSet, or a Result. How does one method do all that?	<b>It has no behaviour of its own. It asks the type you named to build ITSELF from your iterator &mdash; <code>FromIterator</code>.</b><br><br><code>fn collect&lt;B: FromIterator&lt;Self::Item&gt;&gt;(self) -&gt; B</code>. The work lives in <code>B</code>'s impl, not in <code>collect</code>. That is also why the target type must be stated: it IS the choice of algorithm.<br><br>A pair-yielding iterator collects into a <code>HashMap</code>; a <code>char</code>-yielding one into a <code>String</code>; and your own type can implement <code>FromIterator</code> and join in.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::collections::{HashMap, HashSet};<br><br>fn main() {<br>    let v: Vec&lt;i32&gt;          = (1..=3).collect();<br>    let s: String            = &quot;abc&quot;.chars().rev().collect();<br>    let set: HashSet&lt;i32&gt;    = vec![1, 1, 2].into_iter().collect();<br>    let m: HashMap&lt;&amp;str, i32&gt; = vec![(&quot;a&quot;, 1), (&quot;b&quot;, 2)].into_iter().collect();<br>    println!(&quot;{v:?} {s} {} {}&quot;, set.len(), m[&quot;a&quot;]);<br>}<br><br>// [1, 2, 3] cba 2 1</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/collect_and_fromiterator/index.html">collect_and_fromiterator</a></div>	rust iterators core
You have <code>Vec&lt;&amp;str&gt;</code> and want <code>Vec&lt;i32&gt;</code>, failing loudly if any row is not a number. Write it.	<b><code>let ns: Result&lt;Vec&lt;i32&gt;, _&gt; = rows.iter().map(|s| s.parse::&lt;i32&gt;()).collect();</code></b><br><br><b>An iterator of <code>Result</code>s collects into ONE <code>Result</code> of a Vec</b> &mdash; and it short-circuits, stopping at the first <code>Err</code> rather than parsing the rest. Same trick for <code>Option</code>.<br><br>Note where the type annotation goes: <code>Result&lt;Vec&lt;_&gt;, _&gt;</code>, not <code>Vec&lt;Result&lt;_, _&gt;&gt;</code> &mdash; that other spelling is also legal and gives you every row's outcome separately. The two are one character apart and mean completely different things.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let rows = [&quot;1&quot;, &quot;2&quot;, &quot;3&quot;];<br>    let all: Result&lt;Vec&lt;i32&gt;, _&gt; = rows.iter().map(|s| s.parse::&lt;i32&gt;()).collect();<br>    println!(&quot;{all:?}&quot;);<br><br>    let bad = [&quot;1&quot;, &quot;x&quot;, &quot;3&quot;];<br>    let some: Result&lt;Vec&lt;i32&gt;, _&gt; = bad.iter().map(|s| s.parse::&lt;i32&gt;()).collect();<br>    println!(&quot;{}&quot;, some.is_err());<br><br>    let each: Vec&lt;Result&lt;i32, _&gt;&gt; = bad.iter().map(|s| s.parse::&lt;i32&gt;()).collect();<br>    println!(&quot;{}&quot;, each.iter().filter(|r| r.is_ok()).count());<br>}<br><br>// Ok([1, 2, 3])<br>// true<br>// 2</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/collect_and_fromiterator/index.html">collect_and_fromiterator</a></div>	rust iterators result
Why does <code>v.iter().filter(|x| x &gt; 2)</code> not compile, when <code>v</code> is a <code>Vec&lt;i32&gt;</code>?	<b>Because <code>x</code> is a <code>&amp;&amp;i32</code>. <code>iter()</code> yields <code>&amp;i32</code>, and <code>filter</code> hands its closure a reference to that.</b><br><br>Three fixes, all common:<br>• <code>.filter(|&amp;&amp;x| x &gt; 2)</code> &mdash; destructure both layers<br>• <code>.filter(|x| **x &gt; 2)</code> &mdash; dereference twice<br>• <code>.copied()</code> before it, so the stream is <code>i32</code> and the closure sees <code>&amp;i32</code><br><br><code>filter</code> is the odd one out: <code>map</code> takes the item <i>by value</i>, so <code>.map(|x| x * 2)</code> on <code>&amp;i32</code> just works. Only the predicate adapters (<code>filter</code>, <code>take_while</code>, <code>skip_while</code>, <code>position</code>) add the extra <code>&amp;</code>, because they must not consume what they are judging.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3, 4];<br>    println!(&quot;{:?}&quot;, v.iter().filter(|&amp;&amp;x| x &gt; 2).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, v.iter().filter(|x| **x &gt; 2).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, v.iter().copied().filter(|x| *x &gt; 2).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, v.iter().map(|x| x * 2).collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// [3, 4]<br>// [3, 4]<br>// [3, 4]<br>// [2, 4, 6, 8]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators gotcha
What does this print?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let scores = [5, 3, 0, 4, 2, 1];<br>    println!(&quot;{:?}&quot;, scores.iter().filter(|s| **s &lt; 5).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, scores.iter().take_while(|s| **s &lt; 5).collect::&lt;Vec&lt;_&gt;&gt;());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[3, 0, 4, 2, 1]<br>[]</pre><br><b><code>filter</code> tests EVERY item. <code>take_while</code> stops at the FIRST failure and never looks again.</b><br><br>The leading <code>5</code> ends <code>take_while</code> immediately, so the result is empty.<br><br>On <b>sorted</b> data that early stop is exactly what you want and much cheaper. On <b>unsorted</b> data it silently returns a prefix that looks like a plausible answer &mdash; the worst kind of bug, because nothing is wrong-looking.<br><br><code>skip_while</code> is the mirror: it drops the leading run and then keeps everything, failures included.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> identical trap &mdash; <code>itertools.takewhile</code> vs <code>filter</code>, including the sorted/unsorted distinction.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators gotcha
What does this print? <code>rows = ["1", "x", "3"]</code><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let rows = [&quot;1&quot;, &quot;x&quot;, &quot;3&quot;];<br>    let parsed: Vec&lt;i32&gt; = rows.iter().flat_map(|s| s.parse::&lt;i32&gt;()).collect();<br>    println!(&quot;{parsed:?} from {} rows&quot;, rows.len());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[1, 3] from 3 rows</pre><br><b><code>flat_map</code> over a <code>Result</code> DELETES your errors.</b><br><br>A <code>Result</code> is an iterator of length 0 or 1, so this compiles, reads well, and silently drops the failed row. Three rows in, two out &mdash; with the same type and shape as the success case, and nothing anywhere saying a row vanished.<br><br>Legitimate when <i>"skip what does not parse"</i> is genuinely the requirement. A silent data-loss bug when it is not.<br><br>When the failure matters: <code>collect()</code> into a <code>Result&lt;Vec&lt;_&gt;, _&gt;</code> and let the first error stop the chain. Same for <code>filter_map</code> with <code>.ok()</code>.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators gotcha result
Keep only the items a fallible conversion succeeds on, and unwrap them in one step. Which adapter?	<b><code>filter_map(|x| ...)</code> &mdash; the closure returns <code>Option&lt;U&gt;</code>; <code>None</code> drops the item, <code>Some(u)</code> keeps <code>u</code>.</b><br><br>It replaces <code>.filter(...).map(...)</code> where the test and the conversion are the same operation &mdash; and it is the honest spelling of the <code>flat_map</code>-over-<code>Result</code> trap, because <code>.ok()</code> makes the discard visible at the call site.<br><br><code>map_while</code> is the <code>take_while</code>-flavoured cousin: it stops at the first <code>None</code> rather than skipping it.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let rows = [&quot;1&quot;, &quot;x&quot;, &quot;3&quot;];<br>    let kept: Vec&lt;i32&gt; = rows.iter().filter_map(|s| s.parse().ok()).collect();<br>    println!(&quot;{kept:?}&quot;);<br><br>    let stopped: Vec&lt;i32&gt; = rows.iter().map_while(|s| s.parse().ok()).collect();<br>    println!(&quot;{stopped:?}&quot;);<br>}<br><br>// [1, 3]<br>// [1]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> no direct equivalent &mdash; you write a comprehension with an <code>if</code>, or <code>filter(None, map(f, xs))</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators option
What does this print? (where does <code>enumerate</code> sit relative to <code>filter</code>?)<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = [&quot;a&quot;, &quot;b&quot;, &quot;c&quot;];<br>    let after: Vec&lt;_&gt; = v.iter().filter(|s| **s != &quot;a&quot;).enumerate().collect();<br>    println!(&quot;{after:?}&quot;);<br>    let before: Vec&lt;_&gt; = v.iter().enumerate().filter(|(_, s)| **s != &quot;a&quot;).collect();<br>    println!(&quot;{before:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[(0, &quot;b&quot;), (1, &quot;c&quot;)]<br>[(1, &quot;b&quot;), (2, &quot;c&quot;)]</pre><br><b><code>enumerate</code> counts the items IT sees &mdash; so after a <code>filter</code> the index is a position in the filtered stream, not in the original.</b><br><br>Put <code>enumerate</code> <b>first</b> when you want the index in the source data (line numbers in a file, row numbers in a table), and destructure the tuple in the later closures.<br><br>This is the single most common off-by-index bug in Rust iterator chains, and it never fails to compile.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> exactly the same &mdash; <code>enumerate(x for x in xs if ...)</code> vs <code>((i, x) for i, x in enumerate(xs) if ...)</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators gotcha
<code>a.iter().zip(b.iter())</code> where <code>a</code> has 3 items and <code>b</code> has 5. How many pairs, and how do you undo it?	<b>Three. <code>zip</code> stops when the SHORTER one runs out &mdash; silently, no error.</b><br><br><code>unzip()</code> is the inverse: an iterator of pairs into two collections.<br><br>Because it stops short, <code>zip</code> composes beautifully with an infinite iterator: <code>(1..).zip(names.iter())</code> is a numbered list with no bounds arithmetic.<br><br>If the lengths <i>should</i> match, check first &mdash; nothing here will tell you they did not.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a = [1, 2, 3];<br>    let b = [&quot;x&quot;, &quot;y&quot;, &quot;z&quot;, &quot;w&quot;, &quot;v&quot;];<br>    let pairs: Vec&lt;_&gt; = a.iter().zip(b.iter()).collect();<br>    println!(&quot;{pairs:?}&quot;);<br><br>    let (nums, names): (Vec&lt;i32&gt;, Vec&lt;&amp;str&gt;) = pairs.into_iter().map(|(n, s)| (*n, *s)).unzip();<br>    println!(&quot;{nums:?} {names:?}&quot;);<br><br>    let numbered: Vec&lt;_&gt; = (1..).zip([&quot;a&quot;, &quot;b&quot;]).collect();<br>    println!(&quot;{numbered:?}&quot;);<br>}<br><br>// [(1, &quot;x&quot;), (2, &quot;y&quot;), (3, &quot;z&quot;)]<br>// [1, 2, 3] [&quot;x&quot;, &quot;y&quot;, &quot;z&quot;]<br>// [(1, &quot;a&quot;), (2, &quot;b&quot;)]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>zip</code> also stops at the shortest; <code>itertools.zip_longest</code> is the other behaviour, and Rust has no std equivalent.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators
<code>.rev()</code> and <code>.len()</code> are not on every iterator. Why not?	<b>They are two extra traits, each a PROMISE about the sequence that <code>next</code> alone cannot make: <code>DoubleEndedIterator</code> and <code>ExactSizeIterator</code>.</b><br><br><code>rev()</code> needs a <code>next_back()</code> &mdash; the sequence must be walkable from both ends. A slice can. A <code>HashMap</code> iterator cannot, and a lazily-read file cannot.<br><br><code>len()</code> needs the count to be known without walking. <code>filter</code> destroys that promise, which is why <code>v.iter().filter(..).len()</code> is <code>E0599</code> while <code>v.iter().filter(..).count()</code> works &mdash; <code>count()</code> walks and is O(n).<br><br>Checked at compile time, so you cannot get this wrong at runtime.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    println!(&quot;{:?}&quot;, v.iter().rev().collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{}&quot;, v.iter().len());<br>    println!(&quot;{}&quot;, v.iter().filter(|x| **x &gt; 1).count());<br>    println!(&quot;{:?}&quot;, v.iter().rev().next());<br>}<br><br>// [3, 2, 1]<br>// 3<br>// 2<br>// Some(3)</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/double_ended_and_exact_size/index.html">double_ended_and_exact_size</a></div>	rust iterators traits
<code>v.iter().windows(2)</code> &mdash; what happens?	<b><code>E0599</code>: no method named <code>windows</code>. <code>windows</code> and <code>chunks</code> are SLICE methods, not iterator adapters.</b><br><br>The reason is structural: both need to look at several items at once, and an iterator that has handed you an item cannot go back for it. Slices can, because the data is all still there.<br><br>Fix: keep the slice (<code>v.windows(2)</code>), or <code>collect()</code> first.<br><br><code>dedup</code> is the same story on <code>Vec</code>, and <code>sort</code> too &mdash; sorting needs the whole sequence at once.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 4];<br>    println!(&quot;{:?}&quot;, v.windows(2).collect::&lt;Vec&lt;_&gt;&gt;());<br>    let collected: Vec&lt;i32&gt; = (1..=3).collect();<br>    println!(&quot;{:?}&quot;, collected.windows(2).collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// [[1, 2], [2, 4]]<br>// [[1, 2], [2, 3]]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/12_Traits/no_method_named/index.html">no_method_named</a></div>	rust iterators gotcha compile-error
When do you reach for <code>fold</code> instead of <code>sum</code>, <code>count</code> or <code>collect</code>?	<b>When the answer is not the same type as the items.</b><br><br><code>fold(init, |acc, item| ...)</code> carries an accumulator through the sequence and hands it back. <code>sum</code>, <code>count</code>, <code>all</code> and <code>collect</code> are each <code>fold</code> with a particular accumulator &mdash; which is worth knowing because it means there is nothing exotic left to learn when a consumer does not exist for your case.<br><br><code>try_fold</code> is the short-circuiting version for a fallible step.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let words = [&quot;ada&quot;, &quot;bo&quot;, &quot;c&quot;];<br>    let total_len = words.iter().fold(0, |acc, w| acc + w.len());<br>    println!(&quot;{total_len}&quot;);<br><br>    let joined = words.iter().fold(String::new(), |mut acc, w| {<br>        if !acc.is_empty() { acc.push(&#x27;-&#x27;); }<br>        acc.push_str(w);<br>        acc<br>    });<br>    println!(&quot;{joined}&quot;);<br>}<br><br>// 6<br>// ada-bo-c</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>functools.reduce(f, xs, init)</code>. <b>JavaScript:</b> <code>Array.prototype.reduce</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/fold_and_reduce/index.html">fold_and_reduce</a></div>	rust iterators
<code>fold</code> vs <code>reduce</code> &mdash; what is the difference, and what does <code>reduce</code> return?	<b><code>reduce</code> has no initial value: it uses the FIRST item as the seed &mdash; so it returns <code>Option&lt;T&gt;</code>, because an empty iterator has no seed.</b><br><br>That also forces the accumulator to be the item type, which is exactly the case <code>fold</code> exists to escape.<br><br>Rule of thumb: an empty input has a sensible answer &rarr; <code>fold</code> with that answer as the seed. An empty input is meaningless ("the longest word of nothing") &rarr; <code>reduce</code>, and the <code>Option</code> is telling you something true.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![3, 1, 2];<br>    println!(&quot;{:?}&quot;, v.iter().copied().reduce(|a, b| a.max(b)));<br>    println!(&quot;{}&quot;, v.iter().fold(0, |a, b| a + b));<br><br>    let empty: Vec&lt;i32&gt; = vec![];<br>    println!(&quot;{:?}&quot;, empty.iter().copied().reduce(|a, b| a + b));<br>    println!(&quot;{}&quot;, empty.iter().fold(0, |a, b| a + b));<br>}<br><br>// Some(3)<br>// 6<br>// None<br>// 0</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/fold_and_reduce/index.html">fold_and_reduce</a></div>	rust iterators option
You want a running total &mdash; every intermediate value, not just the final one. Which adapter?	<b><code>scan(init, |state, item| ...)</code>. <code>fold</code> gives the final accumulator; <code>scan</code> gives every step.</b><br><br>Two details do a lot of work:<br>• the state is <code>&amp;mut</code> &mdash; you mutate <i>through</i> it rather than returning it, unlike <code>fold</code><br>• the closure returns an <code>Option</code>, and returning <code>None</code> <b>ends the iterator</b> &mdash; which makes <code>scan</code> the way to write a <code>take_while</code> whose decision depends on everything seen so far<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let scores = [5, 3, 0, 4, 2, 1];<br>    let running: Vec&lt;i32&gt; = scores.iter()<br>        .scan(0, |total, s| { *total += s; Some(*total) })<br>        .collect();<br>    println!(&quot;{running:?}&quot;);<br><br>    let until_10: Vec&lt;i32&gt; = scores.iter()<br>        .scan(0, |t, s| { *t += s; if *t &gt; 10 { None } else { Some(*t) } })<br>        .collect();<br>    println!(&quot;{until_10:?}&quot;);<br>}<br><br>// [5, 8, 8, 12, 14, 15]<br>// [5, 8, 8]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>itertools.accumulate</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators
Look at the next item WITHOUT consuming it. Which adapter &mdash; and what is the catch?	<b><code>.peekable()</code>, then <code>it.peek()</code>. The catch: <code>peek</code> takes <code>&amp;mut self</code>, so the iterator must be <code>let mut</code>.</b><br><br>It looks like a read-only convenience, but <code>peek</code> has to pull the item and hold it &mdash; that IS a mutation. And a <code>peek</code> whose borrow is still live where you also call <code>next</code> is a borrow error, not a runtime problem.<br><br><code>peek</code> returns <code>Option&lt;&amp;Item&gt;</code>, and repeated peeks return the same item. But you cannot write two of them in one <code>println!</code> &mdash; that is two <code>&amp;mut</code> borrows at once, <code>E0499</code>. Verified the hard way while building this deck.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    let mut it = v.iter().peekable();<br>    println!(&quot;{:?}&quot;, it.peek());<br>    println!(&quot;{:?}&quot;, it.peek());   // same item -- peek does not advance<br>    println!(&quot;{:?}&quot;, it.next());   // now it does<br>    println!(&quot;{:?}&quot;, it.peek());<br>    // println!(&quot;{:?} {:?}&quot;, it.peek(), it.peek());  &lt;- E0499, two &amp;mut at once<br>}<br><br>// Some(1)<br>// Some(1)<br>// Some(1)<br>// Some(2)</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators borrow-checker
Is <code>(1..)</code> legal? What stops it running forever?	<b>Yes &mdash; <code>(1..)</code> is a <code>RangeFrom</code>, an infinite iterator. Laziness is what makes it safe: nothing runs until a consumer asks, and <code>take(n)</code> stops asking.</b><br><br>The danger is only in consumers that need the END: <code>collect</code>, <code>count</code>, <code>sum</code>, <code>last</code>, <code>max</code> on an infinite iterator hang forever. <code>find</code>, <code>any</code>, <code>position</code>, <code>take</code>, <code>zip</code> are all fine because they can stop early.<br><br><code>std::iter::repeat(x)</code>, <code>cycle()</code> and <code>successors</code> are the other infinite generators.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let squares: Vec&lt;i32&gt; = (1..).map(|n| n * n).take(5).collect();<br>    println!(&quot;{squares:?}&quot;);<br>    println!(&quot;{:?}&quot;, (1..).find(|n| n % 7 == 0 &amp;&amp; n % 5 == 0));<br>    println!(&quot;{:?}&quot;, [&quot;a&quot;, &quot;b&quot;].iter().cycle().take(5).collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// [1, 4, 9, 16, 25]<br>// Some(35)<br>// [&quot;a&quot;, &quot;b&quot;, &quot;a&quot;, &quot;b&quot;, &quot;a&quot;]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/iterators_are_lazy/index.html">iterators_are_lazy</a></div>	rust iterators
<code>.cloned()</code> and <code>.copied()</code> &mdash; what do they do, and which should you prefer?	<b>Both turn a stream of <code>&amp;T</code> into a stream of <code>T</code>. <code>copied()</code> requires <code>T: Copy</code>; <code>cloned()</code> only <code>T: Clone</code>.</b><br><br><b>Prefer <code>copied()</code> when it compiles.</b> It is the same machine code, but it cannot silently become expensive &mdash; if someone later changes the element type to a <code>String</code>, <code>copied()</code> stops compiling while <code>cloned()</code> quietly starts allocating per item.<br><br>Both are just <code>.map(|x| *x)</code> / <code>.map(|x| x.clone())</code> with a name that says why.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    let owned: Vec&lt;i32&gt; = v.iter().copied().collect();<br>    println!(&quot;{}&quot;, owned.iter().sum::&lt;i32&gt;());<br><br>    let names = vec![String::from(&quot;ada&quot;)];<br>    let cloned: Vec&lt;String&gt; = names.iter().cloned().collect();<br>    println!(&quot;{cloned:?} {names:?}&quot;);<br>}<br><br>// 6<br>// [&quot;ada&quot;] [&quot;ada&quot;]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators
<code>max()</code>, <code>max_by_key()</code>, <code>max_by()</code> &mdash; when each? And what about floats?	<b><code>max()</code> needs <code>Ord</code>. <code>max_by_key(|x| ...)</code> compares a derived key. <code>max_by(|a, b| ...)</code> takes a comparator. All return <code>Option</code>.</b><br><br><b>Floats have no <code>Ord</code></b> (because of <code>NaN</code>), so <code>max()</code> will not compile on <code>f64</code>. Use <code>max_by(|a, b| a.total_cmp(b))</code>.<br><br>Tie-break trap: <code>max</code> returns the <b>LAST</b> maximum, <code>min</code> the <b>first</b>. That asymmetry is documented and deliberate, and it bites when the items carry identity.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let words = [&quot;bb&quot;, &quot;a&quot;, &quot;cc&quot;];<br>    println!(&quot;{:?}&quot;, words.iter().max());<br>    println!(&quot;{:?}&quot;, words.iter().max_by_key(|w| w.len()));<br>    println!(&quot;{:?}&quot;, words.iter().min_by_key(|w| w.len()));<br><br>    let f = [2.5f64, 0.5, 1.5];<br>    println!(&quot;{:?}&quot;, f.iter().copied().max_by(|a, b| a.total_cmp(b)));<br>}<br><br>// Some(&quot;cc&quot;)<br>// Some(&quot;cc&quot;)<br>// Some(&quot;a&quot;)<br>// Some(2.5)</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators option gotcha
<code>find</code>, <code>position</code>, <code>find_map</code>, <code>any</code> &mdash; what does each give back?	<b><code>find</code> &rarr; <code>Option&lt;Item&gt;</code> (the thing) · <code>position</code> &rarr; <code>Option&lt;usize&gt;</code> (where) · <code>find_map</code> &rarr; <code>Option&lt;U&gt;</code> (the converted thing) · <code>any</code> &rarr; <code>bool</code></b><br><br>All four short-circuit at the first hit, and all four take <code>&amp;mut self</code> &mdash; so the iterator is left <b>positioned after the match</b> and can be read on.<br><br><code>find_map</code> is the one people re-implement by hand: it is <code>filter_map(..).next()</code>, for "the first row that converts successfully".<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = [&quot;a&quot;, &quot;12&quot;, &quot;b&quot;, &quot;34&quot;];<br>    println!(&quot;{:?}&quot;, v.iter().find(|s| s.len() == 2));<br>    println!(&quot;{:?}&quot;, v.iter().position(|s| s.len() == 2));<br>    println!(&quot;{:?}&quot;, v.iter().find_map(|s| s.parse::&lt;i32&gt;().ok()));<br>    println!(&quot;{}&quot;, v.iter().any(|s| *s == &quot;b&quot;));<br><br>    let mut it = v.iter();<br>    it.find(|s| **s == &quot;b&quot;);<br>    println!(&quot;{:?}&quot;, it.next());<br>}<br><br>// Some(&quot;12&quot;)<br>// Some(1)<br>// Some(12)<br>// true<br>// Some(&quot;34&quot;)</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators option
Split a sequence into two collections by a predicate, in one pass. Which consumer?	<b><code>partition(|x| ...)</code> &mdash; returns a tuple, and you must annotate BOTH halves.</b><br><br><code>let (pass, fail): (Vec&lt;_&gt;, Vec&lt;_&gt;) = it.partition(|x| ...);</code><br><br>Like <code>collect</code>, it is generic over the container, so the type annotation is the instruction. The two halves need not be the same container type.<br><br>For <code>Result</code>s specifically, <code>partition</code> gives you both the successes and the failures &mdash; the honest alternative to <code>flat_map</code> throwing errors away.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = 1..=6;<br>    let (even, odd): (Vec&lt;i32&gt;, Vec&lt;i32&gt;) = v.partition(|n| n % 2 == 0);<br>    println!(&quot;{even:?} {odd:?}&quot;);<br><br>    let rows = [&quot;1&quot;, &quot;x&quot;, &quot;3&quot;];<br>    let (ok, bad): (Vec&lt;_&gt;, Vec&lt;_&gt;) = rows.iter()<br>        .map(|s| s.parse::&lt;i32&gt;())<br>        .partition(|r| r.is_ok());<br>    println!(&quot;{} ok, {} bad&quot;, ok.len(), bad.len());<br>}<br><br>// [2, 4, 6] [1, 3, 5]<br>// 2 ok, 1 bad</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> absent from the stdlib &mdash; the docs give it as an <code>itertools</code> recipe.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators
You want a function to return an iterator chain. What is the return type?	<b><code>impl Iterator&lt;Item = T&gt;</code> &mdash; how a function hands back a chain it cannot name.</b><br><br>The real type is something like <code>Filter&lt;Map&lt;slice::Iter&lt;'_, i32&gt;, closure&gt;, closure&gt;</code>, and closures have no nameable type at all, so writing it out is not merely tedious &mdash; it is impossible.<br><br>Two things go wrong:<br>• <b>a lifetime</b> the opaque type has to capture &mdash; borrowing an argument means <code>impl Iterator&lt;Item = &amp;T&gt; + '_</code><br>• <b>a second branch</b> returning a different concrete type &mdash; <code>impl Trait</code> is ONE type, so an <code>if</code> with two different chains needs <code>Box&lt;dyn Iterator&lt;Item = T&gt;&gt;</code> or <code>Either</code><br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn evens(v: &amp;[i32]) -&gt; impl Iterator&lt;Item = i32&gt; + &#x27;_ {<br>    v.iter().copied().filter(|n| n % 2 == 0)<br>}<br><br>fn maybe(flag: bool) -&gt; Box&lt;dyn Iterator&lt;Item = i32&gt;&gt; {<br>    if flag { Box::new(1..3) } else { Box::new((1..6).filter(|n| n % 2 == 1)) }<br>}<br><br>fn main() {<br>    let v = vec![1, 2, 3, 4];<br>    println!(&quot;{:?}&quot;, evens(&amp;v).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, maybe(false).collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// [2, 4]<br>// [1, 3, 5]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/returning_an_iterator/index.html">returning_an_iterator</a></div>	rust iterators traits
What is the minimum you must write to make your own type an iterator &mdash; and what do you get for it?	<b>An <code>Item</code> type and a <code>next</code> method. That is the whole trait.</b><br><br><code>impl Iterator for T { type Item = X; fn next(&amp;mut self) -&gt; Option&lt;Self::Item&gt; }</code> &mdash; and roughly seventy-five provided methods arrive: <code>map</code>, <code>filter</code>, <code>zip</code>, <code>sum</code>, <code>collect</code>, all of it.<br><br><b>The warning:</b> implement <code>Iterator</code> on a <i>collection</i> and you have built something that empties itself the first time anybody reads it. Collections implement <code>IntoIterator</code> instead, and hand out a separate cursor type &mdash; which is why <code>Vec</code> has <code>vec::IntoIter</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">struct Countdown(u32);<br><br>impl Iterator for Countdown {<br>    type Item = u32;<br>    fn next(&amp;mut self) -&gt; Option&lt;u32&gt; {<br>        if self.0 == 0 { return None; }<br>        self.0 -= 1;<br>        Some(self.0 + 1)<br>    }<br>}<br><br>fn main() {<br>    println!(&quot;{:?}&quot;, Countdown(4).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{}&quot;, Countdown(4).filter(|n| n % 2 == 0).sum::&lt;u32&gt;());<br>}<br><br>// [4, 3, 2, 1]<br>// 6</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/implementing_iterator/index.html">implementing_iterator</a></div>	rust iterators traits
Consume the first 2 items of an iterator, then keep using the SAME iterator. What do you need?	<b><code>.by_ref()</code> &mdash; adapters take <code>self</code> by value, so without it the iterator is moved into the adapter and gone.</b><br><br><code>it.by_ref().take(2)</code> borrows instead of moving, so when that chain is dropped the original <code>it</code> is still usable &mdash; and correctly positioned after the items that were taken.<br><br>The classic use: read a header off a line iterator, then process the rest.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3, 4, 5];<br>    let mut it = v.iter();<br>    let head: Vec&lt;_&gt; = it.by_ref().take(2).collect();<br>    let tail: Vec&lt;_&gt; = it.collect();<br>    println!(&quot;{head:?} {tail:?}&quot;);<br>}<br><br>// [1, 2] [3, 4, 5]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/iterators_are_lazy/index.html">iterators_are_lazy</a></div>	rust iterators ownership
Skip the first n, take every k-th, and take the last n. Which of those three is awkward?	<b><code>skip(n)</code> and <code>step_by(k)</code> are adapters. <b>There is no <code>take_last</code></b>.</b><br><br>Taking from the end needs either a <code>DoubleEndedIterator</code> (<code>.rev().take(n).rev()</code> &mdash; note it reverses the order twice) or the slice: <code>&amp;v[v.len() - n..]</code>.<br><br>The general shape: anything needing to know where the END is fits badly on a forward cursor. That is not an omission, it is the same promise-of-the-sequence idea as <code>DoubleEndedIterator</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v: Vec&lt;i32&gt; = (1..=10).collect();<br>    println!(&quot;{:?}&quot;, v.iter().skip(7).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, v.iter().step_by(3).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, v.iter().rev().take(3).rev().collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, &amp;v[v.len() - 3..]);<br>}<br><br>// [8, 9, 10]<br>// [1, 4, 7, 10]<br>// [8, 9, 10]<br>// [8, 9, 10]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>xs[7:]</code>, <code>xs[::3]</code>, <code>xs[-3:]</code> &mdash; slicing does all three, because a Python list is never a lazy cursor.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/double_ended_and_exact_size/index.html">double_ended_and_exact_size</a></div>	rust iterators
A long chain gives the wrong answer. How do you see what is flowing through the middle of it?	<b><code>.inspect(|x| println!("{x:?}"))</code> &mdash; it passes each item through untouched and lets you look.</b><br><br>Drop it anywhere in a chain without changing the types or the result. Because iterators are lazy, <b>the print order also shows you the interleaving</b>: items go through the whole chain one at a time, not stage by stage &mdash; which is usually the thing that was confusing.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let out: Vec&lt;i32&gt; = (1..=3)<br>        .inspect(|x| println!(&quot;in  {x}&quot;))<br>        .map(|x| x * 10)<br>        .inspect(|x| println!(&quot;out {x}&quot;))<br>        .collect();<br>    println!(&quot;{out:?}&quot;);<br>}<br><br>// in  1<br>// out 10<br>// in  2<br>// out 20<br>// in  3<br>// out 30<br>// [10, 20, 30]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/iterators_are_lazy/index.html">iterators_are_lazy</a></div>	rust iterators debugging
When is a plain <code>for</code> loop the better choice than an iterator chain?	<b>The fluent style wins for transform-and-keep and loses for everything with real control flow in it.</b><br><br>Reach for a loop when you have:<br>• <b>an error that names its row</b> &mdash; "line 47 is malformed"; a chain has no line number in scope<br>• <b>a work list that grows while you drain it</b> &mdash; you cannot push onto what you are iterating<br>• <b>a break out of two levels</b> &mdash; there is no labelled break in a closure<br>• <b>one pass answering three questions</b> &mdash; three chains means three passes, or an awkward <code>fold</code> over a tuple<br><br>This is not a performance argument. Both compile to roughly the same thing. It is about which one a reader can follow.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/when_a_loop_beats_a_chain/index.html">when_a_loop_beats_a_chain</a></div>	rust iterators style
What is <code>[].iter().sum::&lt;i32&gt;()</code>? And <code>.max()</code> on the same?	<b><code>sum</code> is <code>0</code>. <code>max</code> is <code>None</code>.</b><br><br><code>sum</code> and <code>product</code> have an identity element (0 and 1), so they answer for an empty sequence. <code>max</code>, <code>min</code>, <code>reduce</code>, <code>last</code>, <code>first</code> do not, so they return <code>Option</code>.<br><br>Reading the return type tells you which kind you are holding &mdash; and an <code>Option</code> coming back is the API telling you the empty case is real and needs a decision.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let e: Vec&lt;i32&gt; = vec![];<br>    println!(&quot;{} {}&quot;, e.iter().sum::&lt;i32&gt;(), e.iter().product::&lt;i32&gt;());<br>    println!(&quot;{:?} {:?}&quot;, e.iter().max(), e.iter().last());<br>    println!(&quot;{}&quot;, e.iter().copied().max().unwrap_or(i32::MIN));<br>}<br><br>// 0 1<br>// None None<br>// -2147483648</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/fold_and_reduce/index.html">fold_and_reduce</a></div>	rust iterators option
Join two iterators end to end, and flatten a nested one. Which two adapters?	<b><code>a.chain(b)</code> runs <code>a</code> then <code>b</code>. <code>.flatten()</code> unwraps one level of nesting.</b><br><br><code>flat_map(f)</code> is <code>map(f).flatten()</code> &mdash; use it when the closure produces the sequence.<br><br><code>flatten()</code> also works on an iterator of <code>Option</code>s or <code>Result</code>s, silently dropping the empty ones. Convenient, and the same silent-data-loss hazard as <code>flat_map</code> over a <code>Result</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a = [1, 2];<br>    let b = [3, 4];<br>    println!(&quot;{:?}&quot;, a.iter().chain(b.iter()).collect::&lt;Vec&lt;_&gt;&gt;());<br><br>    let nested = vec![vec![1, 2], vec![3]];<br>    println!(&quot;{:?}&quot;, nested.iter().flatten().collect::&lt;Vec&lt;_&gt;&gt;());<br><br>    let words = [&quot;ab&quot;, &quot;cd&quot;];<br>    println!(&quot;{:?}&quot;, words.iter().flat_map(|s| s.chars()).collect::&lt;Vec&lt;_&gt;&gt;());<br><br>    let opts = [Some(1), None, Some(3)];<br>    println!(&quot;{:?}&quot;, opts.iter().flatten().collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// [1, 2, 3, 4]<br>// [1, 2, 3]<br>// [&#x27;a&#x27;, &#x27;b&#x27;, &#x27;c&#x27;, &#x27;d&#x27;]<br>// [1, 3]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/adapters_by_job/index.html">adapters_by_job</a></div>	rust iterators
A chain gives <code>E0599: no method named ...</code>. What are the two usual causes?	<b>(1) The method is on the <b>collection</b>, not the iterator (<code>windows</code>, <code>chunks</code>, <code>sort</code>, <code>dedup</code>, <code>len</code>).<br>(2) A <b>trait is not in scope</b> &mdash; the method exists but its trait is not <code>use</code>d.</b><br><br>Cause 2 is the one that reads as a lie, because the method is right there in the docs. <code>Itertools</code> methods need <code>use itertools::Itertools;</code>; <code>read_line</code> needs <code>use std::io::BufRead;</code>.<br><br>rustc usually tells you: <i>"items from traits can only be used if the trait is in scope"</i>, and names the <code>use</code> line. Read to the end of the error.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::io::Write;<br><br>fn main() {<br>    let mut buf: Vec&lt;u8&gt; = Vec::new();<br>    // write! needs the Write trait in scope -- without the `use`, E0599<br>    write!(buf, &quot;hi {}&quot;, 42).unwrap();<br>    println!(&quot;{}&quot;, String::from_utf8(buf).unwrap());<br>}<br><br>// hi 42</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/12_Traits/no_method_named/index.html">no_method_named</a></div>	rust iterators traits compile-error
