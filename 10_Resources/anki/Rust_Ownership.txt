#separator:tab
#html:true
#notetype:Basic
#deck:Rust::Ownership
#tags column:3
What actually HAPPENS to the data when a value is moved?	<b>Nothing. The bytes stay exactly where they are.</b><br><br>A move is not a copy and not a free &mdash; it is a <b>transfer of responsibility</b>. What changes is <i>who will free the value, and therefore when</i>. The old name is marked unusable at compile time; no instruction runs.<br><br>For a <code>String</code>, the three-word header (pointer, len, capacity) is copied to the new stack slot. The heap text is never touched, never relocated, never duplicated.<br><br>So "moves are expensive" is backwards: a move is at most a few words, and is usually optimised away.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a = String::from(&quot;hello world&quot;);<br>    let heap_before = a.as_ptr();<br>    let b = a;                      // move<br>    println!(&quot;{}&quot;, heap_before == b.as_ptr());<br>    println!(&quot;{b}&quot;);<br>}<br><br>// true<br>// hello world</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> nothing corresponds &mdash; every binding is a new reference to the same object and the GC decides when it dies. In Rust the compiler decides, statically, and a move is how it knows.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/ownership_and_moves/index.html">ownership_and_moves</a></div>	rust ownership core
Why does the compiler explain a move error by naming a trait you never wrote?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn take(s: String) -&gt; usize { s.len() }<br><br>fn main() {<br>    let s = String::from(&quot;hi&quot;);<br>    take(s);<br>    println!(&quot;{s}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0382]  -- it does not compile</pre><br><b><code>E0382</code>: <i>value used after move</i> &mdash; and the note says <i>"which does not implement the <code>Copy</code> trait"</i>.</b><br><br>Moving is what <b>every</b> type does by default, so there is nothing to implement to make a type move. <code>Copy</code> is the <b>opt-out that stops it</b>. That is why the compiler explains a move as an <b>absence</b> &mdash; it is telling you which escape hatch you did not have.<br><br>There is no <code>Move</code> trait to go looking for.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/no_move_trait/index.html">no_move_trait</a></div>	rust ownership compile-error core
Which types are <code>Copy</code>? State the rule, not a list.	<b>A type is <code>Copy</code> if it is entirely made of bits that can be duplicated with no bookkeeping &mdash; and every one of its fields is also <code>Copy</code>.</b><br><br>Yes: all the integers and floats, <code>bool</code>, <code>char</code>, <code>&amp;T</code> (a shared reference), arrays and tuples of <code>Copy</code> things.<br><br>No: <code>String</code>, <code>Vec</code>, <code>Box</code>, and <b><code>&amp;mut T</code></b> &mdash; copying a unique reference would make it not unique, which is the whole point of it.<br><br>Your own struct is <code>Copy</code> only if you write <code>#[derive(Copy, Clone)]</code>, and only if every field allows it. <code>Copy</code> implies <code>Clone</code>, never the reverse.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">#[derive(Copy, Clone, Debug)]<br>struct Point { x: i32, y: i32 }<br><br>fn main() {<br>    let p = Point { x: 1, y: 2 };<br>    let q = p;                    // copy, not move<br>    println!(&quot;{p:?} {q:?}&quot;);<br><br>    let n = 5;<br>    let m = n;<br>    println!(&quot;{n} {m}&quot;);<br>}<br><br>// Point { x: 1, y: 2 } Point { x: 1, y: 2 }<br>// 5 5</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/16_Structs/copy_vs_clone/index.html">copy_vs_clone</a></div>	rust ownership core
Which keyword puts a value on the heap in Rust?	<b>There isn't one. <code>let</code> ALWAYS makes a stack slot.</b><br><br>Whether that slot <i>is</i> the data or a <i>pointer to</i> the data is a property of the <b>type</b>, not of how you declared it. <code>let n = 5</code> puts four bytes on the stack; <code>let s = String::from("hi")</code> puts a three-word header on the stack that points at heap text.<br><br>That is what prices every move, copy and clone you will ever write &mdash; and it is why <code>size_of</code> is a constant per type and never depends on the contents.<br><br><code>Box::new(x)</code> is the explicit heap allocation, and it is a <i>function call</i>, not syntax.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{}&quot;, size_of::&lt;i32&gt;());<br>    println!(&quot;{}&quot;, size_of::&lt;String&gt;());<br>    println!(&quot;{}&quot;, size_of::&lt;Box&lt;[u8; 4096]&gt;&gt;());<br>    println!(&quot;{}&quot;, size_of::&lt;[u8; 4096]&gt;());<br>}<br><br>// 4<br>// 24<br>// 8<br>// 4096</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> everything is on the heap and every name is a pointer &mdash; which is why Python needs no such distinction and gives you no way to make one.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/stack_and_heap/index.html">stack_and_heap</a></div>	rust ownership memory core
You print <code>{:p}</code> for a <code>&amp;String</code>. Whose address is it &mdash; the header or the text?	<b>The three-word header on the STACK. Never the text on the heap.</b><br><br>So after a move the printed number changes &mdash; without a single byte of the text relocating. Printing addresses to "see" a move therefore shows you the opposite of what people expect it to.<br><br><code>s.as_ptr()</code> is the one that gives the heap address, and <b>that</b> is the number that stays the same across a move.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a = String::from(&quot;hello world&quot;);<br>    let heap = a.as_ptr();<br>    let stack_a = &amp;a as *const String;<br>    let b = a;<br>    let stack_b = &amp;b as *const String;<br>    println!(&quot;heap same:  {}&quot;, heap == b.as_ptr());<br>    println!(&quot;stack same: {}&quot;, stack_a == stack_b);<br>}<br><br>// heap same:  true<br>// stack same: false</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/what_an_address_shows/index.html">what_an_address_shows</a></div>	rust ownership memory
State the borrowing rule in one line.	<b>Many readers, or one writer. Never both at once.</b><br><br>Any number of <code>&amp;T</code>, OR exactly one <code>&amp;mut T</code>, for any given value at any given moment.<br><br>It is not a rule about <i>lines of code</i> &mdash; it is about <b>where the compiler thinks each borrow ended</b>, which is the part that actually decides whether your code compiles.<br><br>The payoff is bigger than memory safety: it is what makes data races impossible, and what lets the compiler reason about aliasing hard enough to optimise well.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    let a = &amp;v;<br>    let b = &amp;v;                 // many readers: fine<br>    println!(&quot;{} {}&quot;, a.len(), b.len());<br><br>    let m = &amp;mut v;             // the reads above have ended, so this is fine<br>    m.push(4);<br>    println!(&quot;{v:?}&quot;);<br>}<br><br>// 3 3<br>// [1, 2, 3, 4]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership borrow-checker core
When does a borrow END &mdash; at the closing brace, or somewhere else?	<b>At its LAST USE, not at the end of the scope.</b> (Non-lexical lifetimes, since Rust 2018.)<br><br>This is the single most useful thing to know about the borrow checker, because it turns a large class of "obviously fine" code from an error into a non-event &mdash; and because pre-2018 advice on the internet still says otherwise.<br><br>So the fix for a borrow error is often just <b>to move the last use earlier</b>, or to introduce a <code>{ }</code> block that ends the borrow explicitly. You rarely need to clone.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut s = String::from(&quot;hi&quot;);<br>    let r = &amp;s;<br>    println!(&quot;{r}&quot;);        // last use of r -- the borrow ends HERE<br>    s.push_str(&quot; there&quot;);   // so this is fine<br>    println!(&quot;{s}&quot;);<br>}<br><br>// hi<br>// hi there</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership borrow-checker core
Does this compile?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    let a = &amp;mut v;<br>    let b = &amp;mut v;<br>    a.push(4);<br>    b.push(5);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0499]  -- it does not compile</pre><br><b>No &mdash; <code>E0499</code>: cannot borrow <code>v</code> as mutable more than once at a time.</b><br><br>Note WHY it fails here but not always: <code>a</code> is still used after <code>b</code> is created, so its borrow is still live. Delete the <code>a.push(4)</code> line and it compiles &mdash; the first borrow would have ended before the second began.<br><br>That is the practical shape of almost every <code>E0499</code>: not "two mutable borrows exist" but "two mutable borrows OVERLAP".<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership borrow-checker compile-error
Does this compile?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    for x in &amp;v {<br>        if *x == 2 { v.push(99); }<br>    }<br>    println!(&quot;{v:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0502]  -- it does not compile</pre><br><b>No &mdash; <code>E0502</code>: cannot borrow <code>v</code> as mutable because it is also borrowed as immutable.</b><br><br><code>for x in &amp;v</code> holds a shared borrow for the whole loop. Pushing could reallocate and move every element, invalidating the loop's own cursor.<br><br>Fixes: collect what you want to add and push it <b>after</b> the loop; or loop over indices <code>0..v.len()</code>; or use <code>retain</code> / <code>drain</code> if you are removing.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> the same code runs and silently misbehaves &mdash; mutating a list while iterating skips elements. <b>C++:</b> it is undefined behaviour. Rust makes it a compile error.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership borrow-checker compile-error gotcha
Does this compile?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn make&lt;&#x27;a&gt;() -&gt; &amp;&#x27;a str {<br>    let s = String::from(&quot;hi&quot;);<br>    &amp;s<br>}<br><br>fn main() { println!(&quot;{}&quot;, make()); }</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0515]  -- it does not compile</pre><br><b>No &mdash; <code>E0515</code>: cannot return reference to local variable <code>s</code>.</b><br><br>The <code>&lt;'a&gt;</code> does not save it, and this is exactly why: <b>a lifetime annotation does not make anything live longer.</b> <code>s</code> is dropped at the closing brace regardless of what you named.<br><br>Fixes: return the <code>String</code> itself (give up ownership), or return <code>&amp;'static str</code> if the text is a literal, or take a reference <i>in</i> and return one derived from it.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/lifetime_annotations/index.html">lifetime_annotations</a></div>	rust ownership lifetimes compile-error
What does <code>&lt;'a&gt;</code> actually DO?	<b>Nothing at runtime. It NAMES a relationship between lifetimes that already exist, so the compiler can refuse the arrangement where one outlives the other.</b><br><br>It is a constraint you are stating, not a capability you are granting. <code>fn longest&lt;'a&gt;(a: &amp;'a str, b: &amp;'a str) -&gt; &amp;'a str</code> says "the result may not outlive either input" &mdash; it does not extend either one.<br><br>Lifetimes are erased entirely before code generation. There is no lifetime in the binary.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn longest&lt;&#x27;a&gt;(a: &amp;&#x27;a str, b: &amp;&#x27;a str) -&gt; &amp;&#x27;a str {<br>    if a.len() &gt;= b.len() { a } else { b }<br>}<br><br>fn main() {<br>    let x = String::from(&quot;hello&quot;);<br>    let out;<br>    {<br>        let y = String::from(&quot;hi&quot;);<br>        out = longest(&amp;x, &amp;y).len();   // use it while BOTH are alive<br>    }<br>    println!(&quot;{out}&quot;);<br>}<br><br>// 5</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/lifetime_annotations/index.html">lifetime_annotations</a></div>	rust ownership lifetimes
Why do most functions taking <code>&amp;str</code> need no lifetime annotation at all?	<b>Lifetime elision &mdash; three rules the compiler applies before asking you.</b><br><br>1. Each elided <b>input</b> reference gets its own fresh lifetime.<br>2. If there is <b>exactly one</b> input lifetime, it is given to every elided output.<br>3. If one of the inputs is <code>&amp;self</code> or <code>&amp;mut self</code>, <b>its</b> lifetime is given to every elided output.<br><br>Rule 3 is why methods almost never need annotations. You are forced to write them when there are two input references and an output reference &mdash; the compiler will not guess which one the result came from.<br><br>So a lifetime error is usually not "I need to learn lifetimes" but "the elision rules ran out".<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn first_word(s: &amp;str) -&gt; &amp;str {          // rule 2: one input, so output borrows it<br>    s.split_whitespace().next().unwrap_or(&quot;&quot;)<br>}<br><br>struct Doc { text: String }<br>impl Doc {<br>    fn head(&amp;self) -&gt; &amp;str { &amp;self.text[..2] }   // rule 3: borrows self<br>}<br><br>fn main() {<br>    println!(&quot;{}&quot;, first_word(&quot;hello there&quot;));<br>    println!(&quot;{}&quot;, Doc { text: &quot;abcd&quot;.into() }.head());<br>}<br><br>// hello<br>// ab</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/how_to_learn_lifetimes/index.html">how_to_learn_lifetimes</a></div>	rust ownership lifetimes
"Just <code>.clone()</code> it" gets you past most borrow errors. Name the case where it compiles, runs, and is WRONG.<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn add(v: &amp;mut Vec&lt;i32&gt;, n: i32) { v.push(n); }<br>fn add_cloned(mut v: Vec&lt;i32&gt;, n: i32) { v.push(n); }<br><br>fn main() {<br>    let mut a = vec![5, 3];<br>    add(&amp;mut a, 4);<br>    println!(&quot;{a:?}&quot;);<br><br>    let b = vec![5, 3];<br>    add_cloned(b.clone(), 4);<br>    println!(&quot;{b:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[5, 3, 4]<br>[5, 3]</pre><br><b>Cloning to escape a MUTATION error compiles, runs, and silently does nothing.</b><br><br>The push landed &mdash; on the clone, which was then dropped. No error, no warning, no panic. The compiler cannot help here, because mutating a copy is a perfectly legal thing to want.<br><br>So the advice <i>"copy and clone everything, obey the compiler"</i> is good &mdash; it is the fastest way past the wall &mdash; but with one amendment: <b>it works for READ errors and betrays you on WRITE errors.</b> If you cloned to fix a borrow error and the value should have changed, check that it did.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/how_to_learn_lifetimes/index.html">how_to_learn_lifetimes</a></div>	rust ownership gotcha core
<code>Vec::clone</code> and <code>Rc::clone</code> are the same method name. What is the difference?	<b>Opposite meanings. <code>Vec::clone</code> duplicates the DATA. <code>Rc::clone</code> duplicates a pointer and bumps a COUNT &mdash; and never touches the data.</b><br><br><code>Rc::clone</code> is the cheapest <code>.clone()</code> in Rust and the most commonly misread one: people avoid it thinking it is expensive, and people call it thinking they got an independent copy. Neither.<br><br>House style: write <code>Rc::clone(&amp;a)</code> rather than <code>a.clone()</code>, precisely so the reader can see at the call site which of the two things is happening.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::rc::Rc;<br><br>fn main() {<br>    let v = vec![1, 2, 3];<br>    let w = v.clone();<br>    println!(&quot;{}&quot;, v.as_ptr() == w.as_ptr());<br><br>    let a = Rc::new(vec![1, 2, 3]);<br>    println!(&quot;{}&quot;, Rc::strong_count(&amp;a));<br>    let b = Rc::clone(&amp;a);<br>    println!(&quot;{} {}&quot;, Rc::strong_count(&amp;a), a.as_ptr() == b.as_ptr());<br>}<br><br>// false<br>// 1<br>// 2 true</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/reference_counting/index.html">reference_counting</a></div>	rust ownership gotcha
<code>Rc</code> vs <code>Arc</code> &mdash; and why is the difference not a performance note?	<b><code>Arc</code> is <code>Rc</code> with an ATOMIC counter &mdash; and that is the reason one of them compiles across a thread boundary and the other does not.</b><br><br><code>Rc</code> is not <code>Send</code>, so passing one to another thread is a compile error, not a race you have to find. Use <code>Rc</code> single-threaded (it is faster), <code>Arc</code> when a value genuinely crosses threads.<br><br>Both give <b>shared, immutable</b> access. To mutate you need an interior-mutability cell inside: <code>Rc&lt;RefCell&lt;T&gt;&gt;</code> single-threaded, <code>Arc&lt;Mutex&lt;T&gt;&gt;</code> across threads. Those two pairings are worth memorising as pairings.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::sync::{Arc, Mutex};<br>use std::thread;<br><br>fn main() {<br>    let counter = Arc::new(Mutex::new(0));<br>    let mut handles = vec![];<br>    for _ in 0..4 {<br>        let c = Arc::clone(&amp;counter);<br>        handles.push(thread::spawn(move || { *c.lock().unwrap() += 1; }));<br>    }<br>    for h in handles { h.join().unwrap(); }<br>    println!(&quot;{}&quot;, counter.lock().unwrap());<br>}<br><br>// 4</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/sharing_across_threads/index.html">sharing_across_threads</a></div>	rust ownership concurrency
<code>RefCell</code> moves the borrow check from compile time to where?	<b>Runtime &mdash; and a violation is a PANIC, not a compile error.</b><br><br><code>borrow()</code> and <code>borrow_mut()</code> keep the same rule (many readers or one writer) with a counter, and <code>borrow_mut()</code> while a borrow is live panics with <code>already borrowed: BorrowMutError</code>.<br><br>So it is a deliberate trade, not a loophole: you take on a runtime failure to express a shape the static checker cannot verify &mdash; typically a graph, an observer list, or a cache.<br><br><code>try_borrow_mut()</code> returns a <code>Result</code> if you would rather not panic.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::cell::RefCell;<br><br>fn main() {<br>    let c = RefCell::new(vec![1, 2]);<br>    c.borrow_mut().push(3);<br>    println!(&quot;{:?}&quot;, c.borrow());<br><br>    let _live = c.borrow();<br>    println!(&quot;{}&quot;, c.try_borrow_mut().is_err());<br>}<br><br>// [1, 2, 3]<br>// true</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/reference_counting/index.html">reference_counting</a></div>	rust ownership
<code>let x = 5; let x = x + 1;</code> vs <code>let mut x = 5; x = x + 1;</code> &mdash; what is physically different?	<b><code>mut</code> writes into the SAME place. A shadow builds a SECOND place and moves the name onto it.</b><br><br>So a shadow may change the type; a <code>mut</code> assignment may not. And the way to prove the two are different is not to print addresses but to <b>take a reference</b>: the borrow checker accepts the shadow and rejects the <code>mut</code> spelling of the same four lines, because the shadow never wrote through the place the reference points at.<br><br>Use a shadow when the new binding is <b>the same concept in a new form</b> (the parsed version of a string). Use a second name when it is a different thing. Never shadow something holding a resource.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let spaces = &quot;   &quot;;<br>    let spaces = spaces.len();      // shadow: &amp;str becomes usize<br>    println!(&quot;{spaces}&quot;);<br><br>    let s = String::from(&quot;hi&quot;);<br>    let r = &amp;s;<br>    let s = s.len();                // shadow -- r is still valid<br>    println!(&quot;{r} {s}&quot;);<br>}<br><br>// 3<br>// hi 2</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/a_name_is_not_a_place/index.html">a_name_is_not_a_place</a></div>	rust ownership shadowing
You shadow a <code>String</code>. Is the old one dropped at that point?	<b>No. Shadowing takes away a NAME, not a VALUE.</b><br><br>The shadowed <code>String</code> is still alive, still owned, still borrowable &mdash; which is why a reference taken <i>before</i> the shadow keeps working <i>after</i> it. It is dropped at the end of the scope, in the normal way, along with everything else.<br><br>Consequence worth knowing: shadowing a <code>MutexGuard</code> or a file handle does <b>not</b> release it early. Use <code>drop(x)</code> or a <code>{ }</code> block for that.<br><br>And: <b>rustc has no lint for a shadowed variable.</b> What gets mistaken for protection is the type error a wrong shadow trips on its way past &mdash; so when the shadow's type matches what it hides, there is nothing between you and a wrong answer.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let s = String::from(&quot;original&quot;);<br>    let r = &amp;s;<br>    let s = String::from(&quot;shadow&quot;);<br>    println!(&quot;{r} / {s}&quot;);<br>}<br><br>// original / shadow</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/shadowing_does_not_drop/index.html">shadowing_does_not_drop</a></div>	rust ownership shadowing gotcha
Three values declared in one scope. In what order do they drop?	<b>Reverse declaration order &mdash; last declared, first dropped.</b><br><br>It has to be that way: a later value may borrow an earlier one, so the earlier one must outlive it. Struct <i>fields</i>, by contrast, drop in <b>declaration</b> order.<br><br>This matters whenever drop has an effect you can observe: a lock released, a file flushed, a transaction committed, a log line written.<br><br><code>drop(x)</code> ends one early. You cannot call <code>x.drop()</code> &mdash; that would leave the compiler planning to drop it a second time, so it is a compile error and the free function is the only door.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">struct Noisy(&amp;&#x27;static str);<br>impl Drop for Noisy {<br>    fn drop(&amp;mut self) { println!(&quot;drop {}&quot;, self.0); }<br>}<br><br>fn main() {<br>    let _a = Noisy(&quot;a&quot;);<br>    let _b = Noisy(&quot;b&quot;);<br>    {<br>        let _inner = Noisy(&quot;inner&quot;);<br>        println!(&quot;-- leaving block --&quot;);<br>    }<br>    println!(&quot;-- leaving main --&quot;);<br>}<br><br>// -- leaving block --<br>// drop inner<br>// -- leaving main --<br>// drop b<br>// drop a</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/scope_is_about_names/index.html">scope_is_about_names</a></div>	rust ownership drop
"It goes out of scope" answers three different questions. What are they, and do they answer at the same moment?	<b>No &mdash; three different moments.</b><br><br>1. <b>When can I still write this name?</b> &rarr; until the closing brace (or until a shadow takes it).<br>2. <b>When is the value freed?</b> &rarr; at the closing brace, in reverse declaration order.<br>3. <b>When does a borrow stop mattering?</b> &rarr; at its <b>last use</b>, which is usually much earlier.<br><br>That is why one phrase explains a drop, a compile error and an unlocked critical section differently &mdash; and why "just add a scope" sometimes fixes a borrow error and sometimes does nothing. If the problem is (3), moving the last use is enough; if it is (2), you need the block.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/scope_is_about_names/index.html">scope_is_about_names</a></div>	rust ownership core
Does this compile?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">struct User { name: String, age: u32 }<br><br>fn main() {<br>    let u = User { name: String::from(&quot;ada&quot;), age: 36 };<br>    let name = u.name;<br>    println!(&quot;{name} {}&quot;, u.age);<br>    let u2 = u;<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0382]  -- it does not compile</pre><br><b>No &mdash; <code>E0382</code>: use of partially moved value <code>u</code>.</b><br><br>But note what DOES work: after <code>let name = u.name;</code> the line <code>println!("{name} {}", u.age)</code> is fine. The struct is <b>partially moved</b> &mdash; the <code>String</code> field is gone, the <code>Copy</code> field is still readable.<br><br>What you cannot do is use <code>u</code> <b>as a whole</b> again: move it, pass it by value, or <code>{:?}</code> it.<br><br>Fixes: <code>u.name.clone()</code>, or borrow <code>&amp;u.name</code>, or destructure the whole struct at once with <code>let User { name, age } = u;</code>.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/ownership_and_moves/index.html">ownership_and_moves</a></div>	rust ownership compile-error
Can you hold <code>&amp;mut</code> to two different FIELDS of the same struct at once?	<b>Yes &mdash; the borrow checker tracks fields separately. But not through a METHOD.</b><br><br><code>&amp;mut p.a</code> and <code>&amp;mut p.b</code> are disjoint borrows and both are fine. <code>p.get_a_mut()</code> and <code>p.get_b_mut()</code> are not, because each takes <code>&amp;mut self</code> &mdash; borrowing the <b>whole</b> struct.<br><br>This is the most common reason people conclude the borrow checker is being unreasonable: the direct field access works and the accessor does not. The usual fixes are to inline the field access, to split the struct, or to return both at once from one method.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">struct Pair { a: Vec&lt;i32&gt;, b: Vec&lt;i32&gt; }<br><br>fn main() {<br>    let mut p = Pair { a: vec![1], b: vec![2] };<br>    let x = &amp;mut p.a;<br>    let y = &amp;mut p.b;      // disjoint field -- fine<br>    x.push(9);<br>    y.push(9);<br>    println!(&quot;{:?} {:?}&quot;, p.a, p.b);<br>}<br><br>// [1, 9] [2, 9]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership borrow-checker
When do you need <code>move</code> on a closure?	<b>When the closure must OUTLIVE the scope that created it &mdash; a thread, a returned closure, anything stored.</b><br><br>Without <code>move</code>, a closure captures by reference where it can, and that reference cannot outlive its scope. <code>move</code> forces every capture to be taken by value.<br><br>Two things it does not mean:<br>• it does not make the closure <code>FnOnce</code> &mdash; that is decided by what the body <i>does</i> with the captures<br>• for a <code>Copy</code> type it copies rather than moves, so the original stays usable<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::thread;<br><br>fn make_adder(n: i32) -&gt; impl Fn(i32) -&gt; i32 {<br>    move |x| x + n<br>}<br><br>fn main() {<br>    let add5 = make_adder(5);<br>    println!(&quot;{}&quot;, add5(10));<br><br>    let name = String::from(&quot;ada&quot;);<br>    let h = thread::spawn(move || format!(&quot;hi {name}&quot;));<br>    println!(&quot;{}&quot;, h.join().unwrap());<br>}<br><br>// 15<br>// hi ada</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/23_Closures/index.html">closures</a></div>	rust ownership closures
What does <code>fn f(s: String)</code> tell the caller, versus <code>fn f(s: &amp;str)</code>?	<b>Taking by value is a claim that you NEED ownership &mdash; you will store it, consume it, or return it transformed.</b><br><br>The parameter type is documentation the compiler enforces:<br>• <code>&amp;str</code> / <code>&amp;[T]</code> &mdash; I will read it and give it back<br>• <code>&amp;mut T</code> &mdash; I will change it in place<br>• <code>T</code> &mdash; I am keeping it; you do not have it any more<br><br>Default to borrowing. Taking <code>String</code> when you only read it forces every caller to clone, and that cost is invisible at the call site.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn read(s: &amp;str) -&gt; usize { s.len() }<br>fn grow(s: &amp;mut String) { s.push(&#x27;!&#x27;); }<br>fn keep(s: String) -&gt; String { s }<br><br>fn main() {<br>    let mut s = String::from(&quot;hi&quot;);<br>    println!(&quot;{}&quot;, read(&amp;s));<br>    grow(&amp;mut s);<br>    println!(&quot;{s}&quot;);<br>    let owned = keep(s);        // s is gone from here on<br>    println!(&quot;{owned}&quot;);<br>}<br><br>// 2<br>// hi!<br>// hi!</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership api-design
You need to move a value OUT of a struct field you only have <code>&amp;mut</code> to. How?	<b><code>std::mem::take(&amp;mut field)</code> &mdash; takes the value and leaves <code>Default::default()</code> behind.</b><br><br>The struct is never left with a hole, so the borrow checker is satisfied. Three in the family:<br>• <code>mem::take(&amp;mut x)</code> &mdash; leaves the default (needs <code>T: Default</code>)<br>• <code>mem::replace(&amp;mut x, val)</code> &mdash; leaves what you supply<br>• <code>mem::swap(&amp;mut a, &amp;mut b)</code> &mdash; exchanges two<br><br><code>Option::take()</code> is the same idea specialised: takes the <code>Some</code>, leaves <code>None</code>. It is how you write a linked list or a state machine that consumes its own state.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::mem;<br><br>struct Buf { rows: Vec&lt;i32&gt; }<br><br>fn drain(b: &amp;mut Buf) -&gt; Vec&lt;i32&gt; { mem::take(&amp;mut b.rows) }<br><br>fn main() {<br>    let mut b = Buf { rows: vec![1, 2, 3] };<br>    println!(&quot;{:?} {:?}&quot;, drain(&amp;mut b), b.rows);<br><br>    let mut slot = Some(String::from(&quot;hi&quot;));<br>    println!(&quot;{:?} {:?}&quot;, slot.take(), slot);<br>}<br><br>// [1, 2, 3] []<br>// Some(&quot;hi&quot;) None</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/ownership_and_moves/index.html">ownership_and_moves</a></div>	rust ownership
One function must return either borrowed or owned text, deciding at runtime. What type?	<b><code>Cow&lt;'a, str&gt;</code> &mdash; an ordinary enum with a <code>Borrowed</code> arm and an <code>Owned</code> arm.</b><br><br>It lets a function avoid deciding in advance, so the common case (nothing to change) allocates nothing and the rare case still works. <code>String::from_utf8_lossy</code> is the std example.<br><br><code>to_mut()</code> is the moment the copy actually happens &mdash; that is the <i>write</i> that "clone on write" is named after. <code>into_owned()</code> forces it.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::borrow::Cow;<br><br>fn clean(s: &amp;str) -&gt; Cow&lt;&#x27;_, str&gt; {<br>    if s.contains(&#x27; &#x27;) { Cow::Owned(s.replace(&#x27; &#x27;, &quot;_&quot;)) } else { Cow::Borrowed(s) }<br>}<br><br>fn main() {<br>    let a = clean(&quot;already_clean&quot;);<br>    let b = clean(&quot;has spaces&quot;);<br>    println!(&quot;{}&quot;, matches!(a, Cow::Borrowed(_)));<br>    println!(&quot;{}&quot;, matches!(b, Cow::Owned(_)));<br>    println!(&quot;{a} {b}&quot;);<br>}<br><br>// true<br>// true<br>// already_clean has_spaces</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/clone_on_write/index.html">clone_on_write</a></div>	rust ownership performance
Name the three jobs <code>Box&lt;T&gt;</code> actually does.	<b>1. Put a large value on the heap so moves stay cheap.<br>2. Give a recursive type a known size.<br>3. Hold a trait object: <code>Box&lt;dyn Trait&gt;</code>.</b><br><br>Job 2 is the one that is a hard requirement rather than a choice: <code>enum List { Cons(i32, List), Nil }</code> has infinite size and will not compile; <code>Box&lt;List&gt;</code> is one pointer, so the size is known.<br><br>Job 3 is how you get a heterogeneous collection &mdash; <code>Vec&lt;Box&lt;dyn Shape&gt;&gt;</code> &mdash; or return one of two different concrete types from one function.<br><br><code>Box</code> is a single owner. Sharing needs <code>Rc</code>/<code>Arc</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">#[derive(Debug)]<br>enum List { Cons(i32, Box&lt;List&gt;), Nil }<br><br>fn main() {<br>    use List::{Cons, Nil};<br>    let l = Cons(1, Box::new(Cons(2, Box::new(Nil))));<br>    println!(&quot;{l:?}&quot;);<br><br>    let shapes: Vec&lt;Box&lt;dyn Fn(i32) -&gt; i32&gt;&gt; = vec![<br>        Box::new(|x| x + 1),<br>        Box::new(|x| x * 2),<br>    ];<br>    println!(&quot;{:?}&quot;, shapes.iter().map(|f| f(10)).collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// Cons(1, Cons(2, Nil))<br>// [11, 20]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/stack_and_heap/index.html">stack_and_heap</a></div>	rust ownership memory
What does <code>&amp;'static T</code> mean &mdash; and what is the common misreading?	<b>It means the reference is valid for the whole program. The misreading is thinking it makes the value live that long.</b><br><br>It is a <b>constraint</b>, like every lifetime. String literals are genuinely <code>&amp;'static str</code> because their bytes are in the binary.<br><br>The trap is <code>T: 'static</code> as a bound, which is a <b>different and much weaker</b> claim: it means "contains no non-static references", which every OWNED type satisfies. A <code>String</code> is <code>'static</code> by that bound and is dropped whenever you like. That is why <code>thread::spawn</code> requires <code>'static</code> and yet accepts a moved <code>String</code> happily.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn needs_static&lt;T: &#x27;static&gt;(_x: T) {}<br><br>fn main() {<br>    let lit: &amp;&#x27;static str = &quot;in the binary&quot;;<br>    println!(&quot;{lit}&quot;);<br>    needs_static(String::from(&quot;owned, and still &#x27;static by the bound&quot;));<br>    println!(&quot;ok&quot;);<br>}<br><br>// in the binary<br>// ok</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/lifetime_annotations/index.html">lifetime_annotations</a></div>	rust ownership lifetimes gotcha
Why does a function taking <code>&amp;str</code> accept a <code>&amp;String</code> with no conversion at the call site?	<b>Deref coercion &mdash; the compiler inserts <code>&amp;*s</code> for you when the types do not match and a <code>Deref</code> impl bridges them.</b><br><br>The chain that matters in practice: <code>&amp;String</code> &rarr; <code>&amp;str</code>, <code>&amp;Vec&lt;T&gt;</code> &rarr; <code>&amp;[T]</code>, <code>&amp;Box&lt;T&gt;</code> &rarr; <code>&amp;T</code>.<br><br>It is also why <code>String</code> appears to have hundreds of methods it does not define &mdash; <code>split</code>, <code>trim</code>, <code>find</code> all live on <code>str</code> and arrive through the same mechanism.<br><br>It goes one way only: a <code>&amp;str</code> will not become a <code>&amp;String</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn read_str(s: &amp;str) -&gt; usize { s.len() }<br>fn read_slice(v: &amp;[i32]) -&gt; usize { v.len() }<br><br>fn main() {<br>    let s = String::from(&quot;hello&quot;);<br>    let v = vec![1, 2, 3];<br>    let b = Box::new(7);<br>    println!(&quot;{} {} {}&quot;, read_str(&amp;s), read_slice(&amp;v), *b);<br>}<br><br>// 5 3 7</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership api-design
What is a slice, in ownership terms?	<b>A borrow of part of something else: a pointer and a length, owning nothing.</b><br><br><code>&amp;v[1..3]</code> does not copy and does not allocate &mdash; it is a view, and it keeps the original borrowed for as long as it lives. That is why you cannot push to the Vec while a slice of it is alive.<br><br>It is also why slices are the right parameter type: they accept a <code>Vec</code>, an array, another slice, or a sub-range of any of them, and they promise the caller you kept nothing.<br><br><code>&amp;str</code> is exactly this for text &mdash; a string slice, no more and no less.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3, 4, 5];<br>    let middle = &amp;v[1..4];<br>    println!(&quot;{middle:?} {}&quot;, middle.len());<br>    println!(&quot;{:?}&quot;, &amp;middle[1..]);<br>    let s = &quot;hello world&quot;;<br>    println!(&quot;{}&quot;, &amp;s[..5]);<br>}<br><br>// [2, 3, 4] 3<br>// [3, 4]<br>// hello</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/borrowing/index.html">borrowing</a></div>	rust ownership
Your borrow error will not go away. Walk the checklist &mdash; in order.	<b>1. <b>Move the last use earlier.</b> Borrows end at last use, so many errors are just an ordering problem.<br>2. <b>Copy the value out</b> rather than holding a reference: <code>let x = v[0];</code> for a <code>Copy</code> type.<br>3. <b>Introduce a <code>{ }</code> block</b> to end a borrow before the next one starts.<br>4. <b>Clone</b> &mdash; but only if you are reading. If you are WRITING, cloning compiles and silently does nothing.<br>5. <b>Restructure</b>: split the struct, return both values from one method, collect-then-mutate.<br>6. <b>Interior mutability</b> (<code>RefCell</code>/<code>Mutex</code>) &mdash; last, and deliberately.</b><br><br>Reaching for 4 or 6 first is what makes Rust feel like a fight. Steps 1&ndash;3 are free and fix most of it.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/how_to_learn_lifetimes/index.html">how_to_learn_lifetimes</a></div>	rust ownership borrow-checker core
