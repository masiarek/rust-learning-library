#separator:tab
#html:true
#notetype:Basic
#deck:Rust::Vec
#tags column:3
What is a <code>Vec&lt;T&gt;</code>, physically? How big is it on the stack?	<b>Three numbers on the stack &mdash; pointer, length, capacity &mdash; and one allocation on the heap. 24 bytes on a 64-bit machine, WHATEVER <code>T</code> is.</b><br><br>A <code>Vec&lt;[u8; 999]&gt;</code> is also 24 bytes. The elements are not in the <code>Vec</code>; the <code>Vec</code> is a receipt for them.<br><br>• <b>len</b> &mdash; how many are initialised (what you almost always want)<br>• <b>capacity</b> &mdash; how many fit before the next allocation (matters exactly once: when you are about to fill it)<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    println!(&quot;{}&quot;, size_of::&lt;Vec&lt;u8&gt;&gt;());<br>    println!(&quot;{}&quot;, size_of::&lt;Vec&lt;[u8; 999]&gt;&gt;());<br>    let v = vec![5, 3, 0];<br>    println!(&quot;{v:?} len {} cap {}&quot;, v.len(), v.capacity());<br>}<br><br>// 24<br>// 24<br>// [5, 3, 0] len 3 cap 3</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> an internal table &mdash; but ABAP hides the header. Here the three numbers are the type.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec core memory
Five ways to make a <code>Vec</code>. Name them and when each is right.	<b><code>Vec::new()</code> · <code>vec![a, b, c]</code> · <code>vec![0; n]</code> · <code>Vec::with_capacity(n)</code> · <code>.collect()</code></b><br><br>• <code>Vec::new()</code> &mdash; empty, <b>allocates nothing</b> until the first push<br>• <code>vec![0; n]</code> &mdash; n copies; needs <code>T: Clone</code><br>• <code>with_capacity(n)</code> &mdash; when you know the size: one allocation instead of several, nothing copied<br>• <code>collect()</code> &mdash; when the values come from an iterator; it calls <code>with_capacity</code> for you if the iterator knows its own length (a range does, a <code>filter</code> does not)<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let a: Vec&lt;i32&gt; = Vec::new();<br>    let b = vec![5, 3, 0];<br>    let c = vec![0u8; 4];<br>    let d: Vec&lt;u32&gt; = (1..=5).collect();<br>    println!(&quot;{} {b:?} {c:?} {d:?}&quot;, a.capacity());<br>}<br><br>// 0 [5, 3, 0] [0, 0, 0, 0] [1, 2, 3, 4, 5]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec
What does this print?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v: Vec&lt;u32&gt; = Vec::new();<br>    for n in 1..=9 {<br>        v.push(n);<br>        print!(&quot;{} &quot;, v.capacity());<br>    }<br>    println!();<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">4 4 4 4 8 8 8 8 16 </pre><br><b>Growth is amortised doubling: 0 &rarr; 4 &rarr; 8 &rarr; 16.</b> Nine pushes cause three reallocations, and each one <b>copies everything already stored</b> into the new buffer.<br><br>Doubling is what makes pushing <i>n</i> items cost O(<i>n</i>) in total instead of O(<i>n</i>&sup2;). The exact sequence is this std's choice, not a promise in the language &mdash; so <b>never assert on a capacity in a test</b>.<br><br><code>Vec::with_capacity(9)</code> gives capacity 9 and one allocation.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec memory performance
<code>v.pop()</code> &mdash; what comes back?	<b><code>Option&lt;T&gt;</code> &mdash; the owned last element, or <code>None</code> if empty.</b><br><br>It removes from the <b>end</b>, which is why it is O(1). Capacity is unchanged.<br><br>Contrast <code>v.last()</code> &rarr; <code>Option&lt;&amp;T&gt;</code>: looks, does not take.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    println!(&quot;{:?} {:?}&quot;, v.pop(), v.last());<br>    v.clear();<br>    println!(&quot;{:?} cap {}&quot;, v.pop(), v.capacity());<br>}<br><br>// Some(3) Some(2)<br>// None cap 3</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>lst.pop()</code> on an empty list <i>raises</i> <code>IndexError</code>. Rust returns <code>None</code>, so the empty case cannot be forgotten.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec option
<code>v[10]</code> vs <code>v.get(10)</code> on a 3-element Vec &mdash; what does each do?	<b><code>v[10]</code> PANICS. <code>v.get(10)</code> returns <code>None</code>.</b><br><br><code>get</code> hands back <code>Option&lt;&amp;T&gt;</code>, so the missing case is in the type and the compiler makes you handle it. Indexing is for when being out of range is a bug you want to hear about loudly.<br><br><code>get_mut</code> is the mutable twin. Both have a slice-taking form: <code>v.get(1..3)</code> &rarr; <code>Option&lt;&amp;[T]&gt;</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    println!(&quot;{}&quot;, v[0]);<br>    println!(&quot;{:?}&quot;, v.get(10));<br>    println!(&quot;{:?}&quot;, v.get(1..3));<br>    match v.get(10) {<br>        Some(x) =&gt; println!(&quot;got {x}&quot;),<br>        None    =&gt; println!(&quot;out of range&quot;),<br>    }<br>}<br><br>// 1<br>// None<br>// Some([2, 3])<br>// out of range</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>lst[10]</code> raises; <code>dict.get</code> is the closest cousin to <code>v.get</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/arrays_and_slices/index.html">arrays_and_slices</a></div>	rust vec option gotcha
Does <code>v[v.len() - 1]</code> work on an empty Vec? And can you write <code>v[-1]</code>?	<b>Neither. There is no negative indexing, and <code>0usize - 1</code> panics on overflow before indexing even happens.</b><br><br>Vec indices are <code>usize</code> &mdash; unsigned. <code>v[-1]</code> will not compile; <code>v[v.len() - 1]</code> compiles and then panics with <code>attempt to subtract with overflow</code> on an empty Vec (in debug; it wraps to a huge number in release).<br><br><b>Use <code>v.last()</code></b> &rarr; <code>Option&lt;&amp;T&gt;</code>. That is the whole reason it exists.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v: Vec&lt;i32&gt; = vec![];<br>    println!(&quot;{:?}&quot;, v.last());<br>    let w = vec![1, 2, 3];<br>    println!(&quot;{:?} {:?}&quot;, w.last(), w.first());<br>}<br><br>// None<br>// Some(3) Some(1)</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>lst[-1]</code> is idiomatic and safe-ish. In Rust the idiom is <code>last()</code>, and it cannot panic.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/arrays_and_slices/index.html">arrays_and_slices</a></div>	rust vec gotcha
<code>iter()</code>, <code>iter_mut()</code>, <code>into_iter()</code> &mdash; what does each yield, and what happens to the Vec?	<b><code>iter()</code> &rarr; <code>&amp;T</code> (Vec survives) · <code>iter_mut()</code> &rarr; <code>&amp;mut T</code> (Vec survives, elements change) · <code>into_iter()</code> &rarr; <code>T</code> (Vec is CONSUMED)</b><br><br>The <code>for</code> loop picks for you:<br>• <code>for x in &amp;v</code> &rarr; <code>iter()</code><br>• <code>for x in &amp;mut v</code> &rarr; <code>iter_mut()</code><br>• <code>for x in v</code> &rarr; <code>into_iter()</code> &mdash; <b><code>v</code> is gone after this loop</b><br><br>With <code>iter_mut</code> you must dereference to assign: <code>*x *= 2</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    let total: i32 = v.iter().sum();<br>    for x in v.iter_mut() { *x *= 10; }<br>    println!(&quot;{total} {v:?}&quot;);<br>    let owned: Vec&lt;String&gt; = vec![&quot;a&quot;.to_string()].into_iter().collect();<br>    println!(&quot;{owned:?}&quot;);<br>}<br><br>// 6 [10, 20, 30]<br>// [&quot;a&quot;]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>LOOP AT itab INTO wa</code> copies each row (like <code>iter()</code> then clone); <code>LOOP AT itab ASSIGNING &lt;fs&gt;</code> is exactly <code>iter_mut()</code> &mdash; a field-symbol IS a <code>&amp;mut</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/index.html">iterators</a></div>	rust vec iterators ownership
Does this compile?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    for x in v {<br>        println!(&quot;{x}&quot;);<br>    }<br>    println!(&quot;{}&quot;, v.len());<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0382]  -- it does not compile</pre><br><b>No &mdash; <code>E0382</code>: borrow of moved value <code>v</code>.</b><br><br><code>for x in v</code> calls <code>into_iter()</code>, which <b>consumes</b> the Vec. After the loop it does not exist.<br><br>Fix: <code>for x in &amp;v</code> &mdash; one character, and it is the one you want 90% of the time. The compiler's own suggestion says so.<br><br>This is the single most common beginner move-error in Rust.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python / ABAP:</b> looping never destroys the collection, so nothing in either language prepares you for this.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/index.html">ownership</a></div>	rust vec ownership compile-error gotcha
Does this compile?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    let first = &amp;v[0];<br>    v.push(4);<br>    println!(&quot;{first}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0502]  -- it does not compile</pre><br><b>No &mdash; <code>E0502</code>: cannot borrow <code>v</code> as mutable because it is also borrowed as immutable.</b><br><br>This is not pedantry. <code>push</code> may reallocate and copy the elements to a new address &mdash; <code>first</code> would then point at freed memory. In C++ this compiles and is the classic iterator-invalidation bug; Rust makes it a compile error.<br><br>Fixes: copy the value out (<code>let first = v[0];</code>), or move the <code>push</code> before the borrow, or end the borrow by using <code>first</code> first.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> mutating a list while iterating it silently skips elements. Same class of bug, caught here at compile time.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/31_C_and_Cpp/iterator_invalidation/index.html">iterator_invalidation</a></div>	rust vec borrow-checker compile-error
Does this compile? <code>v: Vec&lt;String&gt;</code><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![String::from(&quot;a&quot;), String::from(&quot;b&quot;)];<br>    let s: String = v[0];<br>    println!(&quot;{s}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">rustc 1.98 says</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">error[E0507]  -- it does not compile</pre><br><b>No &mdash; <code>E0507</code>: cannot move out of index of <code>Vec&lt;String&gt;</code>.</b><br><br>Indexing gives you a <i>place</i>, not ownership. Moving the <code>String</code> out would leave a hole in the Vec, and <code>Vec</code> has no way to represent a hole.<br><br>Four ways out, cheapest first:<br>• <code>&amp;v[0]</code> &mdash; just borrow it<br>• <code>v[0].clone()</code> &mdash; pay for a copy<br>• <code>v.remove(0)</code> / <code>v.swap_remove(0)</code> &mdash; take it and close the hole<br>• <code>v.into_iter().next()</code> &mdash; consume the whole Vec<br><br>Note it works fine for <code>Vec&lt;i32&gt;</code>: <code>i32</code> is <code>Copy</code>, so nothing moves.<br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/18_Ownership/index.html">ownership</a></div>	rust vec ownership compile-error gotcha
Remove element at index 1. <code>remove</code> vs <code>swap_remove</code> &mdash; what is the trade?	<b><code>remove(i)</code> keeps order, O(n) &mdash; shifts everything after it down.<br><code>swap_remove(i)</code> is O(1) &mdash; moves the LAST element into the hole, order destroyed.</b><br><br>Both return the removed value (not an <code>Option</code>) and both panic if <code>i</code> is out of range.<br><br>Removing many? Neither &mdash; use <code>retain</code>, which is one pass.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut a = vec![&#x27;a&#x27;, &#x27;b&#x27;, &#x27;c&#x27;, &#x27;d&#x27;];<br>    println!(&quot;{:?} {a:?}&quot;, a.remove(1));<br><br>    let mut b = vec![&#x27;a&#x27;, &#x27;b&#x27;, &#x27;c&#x27;, &#x27;d&#x27;];<br>    println!(&quot;{:?} {b:?}&quot;, b.swap_remove(1));<br>}<br><br>// &#x27;b&#x27; [&#x27;a&#x27;, &#x27;c&#x27;, &#x27;d&#x27;]<br>// &#x27;b&#x27; [&#x27;a&#x27;, &#x27;d&#x27;, &#x27;c&#x27;]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>DELETE itab INDEX 2</code> is <code>remove</code> &mdash; and has the same O(n) shift.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec performance
Delete every element that fails a test, <b>in place</b>, in one pass. Which method &mdash; and mind the polarity.	<b><code>v.retain(|x| ...)</code> &mdash; it KEEPS the elements where the closure is true.</b><br><br>The polarity is the trap: it is named for what survives, not what goes. One pass, O(n), no allocation, order preserved.<br><br><code>retain_mut</code> lets the closure also modify as it decides. <code>extract_if</code> gives you the removed ones back as an iterator.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3, 4, 5, 6];<br>    v.retain(|n| n % 2 == 0);<br>    println!(&quot;{v:?}&quot;);<br><br>    let mut names = vec![&quot;ada&quot;.to_string(), &quot;&quot;.to_string(), &quot;bob&quot;.to_string()];<br>    names.retain(|s| !s.is_empty());<br>    println!(&quot;{names:?}&quot;);<br>}<br><br>// [2, 4, 6]<br>// [&quot;ada&quot;, &quot;bob&quot;]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>DELETE itab WHERE ...</code> &mdash; same one-pass operation, but stated as what to REMOVE. Rust states what to KEEP. Invert the condition.<br><b>Python:</b> <code>lst[:] = [x for x in lst if ...]</code> &mdash; and note the <code>[:]</code>, without which you rebind instead of mutating.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec gotcha
What does this print?<pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 1, 2, 1];<br>    v.dedup();<br>    println!(&quot;{v:?}&quot;);<br><br>    let mut w = vec![1, 2, 1, 2, 1];<br>    w.sort();<br>    w.dedup();<br>    println!(&quot;{w:?}&quot;);<br>}</pre>	<div style="opacity:.6; font-size:.8em; margin-top:8px;">it prints</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">[1, 2, 1, 2, 1]<br>[1, 2]</pre><br><b><code>dedup()</code> only removes <b>CONSECUTIVE</b> duplicates.</b> Unsorted, it does almost nothing.<br><br><code>sort()</code> then <code>dedup()</code> is the unique-elements idiom, O(n log n).<br><br>Variants: <code>dedup_by_key(|x| ...)</code>, <code>dedup_by(|a, b| ...)</code>.<br><br>Order matters and you cannot sort? Then it is a <code>HashSet</code> job, not a <code>Vec</code> one.<br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>DELETE ADJACENT DUPLICATES FROM itab</code> &mdash; <i>identical</i> trap, and the same fix: <code>SORT</code> first. If you have ever been bitten in ABAP, you already know this card.<br><b>Python:</b> <code>set(lst)</code> is the usual move, but it loses order; <code>list(dict.fromkeys(lst))</code> keeps it.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec gotcha abap
Sort a Vec: by natural order, by a key, and by a custom rule. And what about <code>Vec&lt;f64&gt;</code>?	<b><code>v.sort()</code> · <code>v.sort_by_key(|x| ...)</code> · <code>v.sort_by(|a, b| ...)</code></b><br><br><code>sort()</code> is stable and needs <code>T: Ord</code>. <code>sort_unstable()</code> is faster and does not preserve the order of equals &mdash; the right default when elements have no identity beyond their value.<br><br><b>Floats do not implement <code>Ord</code></b> (because of <code>NaN</code>), so <code>v.sort()</code> will not compile on <code>Vec&lt;f64&gt;</code>. Use <code>v.sort_by(f64::total_cmp)</code>.<br><br>Descending: <code>sort_by(|a, b| b.cmp(a))</code>, or sort then <code>reverse()</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![3, 1, 2];<br>    v.sort();<br>    println!(&quot;{v:?}&quot;);<br><br>    let mut words = vec![&quot;ccc&quot;, &quot;a&quot;, &quot;bb&quot;];<br>    words.sort_by_key(|s| s.len());<br>    println!(&quot;{words:?}&quot;);<br><br>    let mut f = vec![2.5, 0.5, 1.5];<br>    f.sort_by(f64::total_cmp);<br>    println!(&quot;{f:?}&quot;);<br><br>    v.sort_by(|a, b| b.cmp(a));<br>    println!(&quot;{v:?}&quot;);<br>}<br><br>// [1, 2, 3]<br>// [&quot;a&quot;, &quot;bb&quot;, &quot;ccc&quot;]<br>// [0.5, 1.5, 2.5]<br>// [3, 2, 1]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>lst.sort(key=len)</code> &rarr; <code>sort_by_key(|s| s.len())</code>. Same idea, and Rust's is also stable.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec sorting
<code>v.binary_search(&amp;3)</code> &mdash; what does it return, and what is the precondition?	<b><code>Result&lt;usize, usize&gt;</code> &mdash; <code>Ok(i)</code> where it is, <code>Err(i)</code> where it <i>would</i> go. The Vec MUST already be sorted.</b><br><br>On an unsorted Vec it returns a meaningless answer with no error &mdash; a silent wrong result, not a panic.<br><br>The <code>Err(i)</code> is not a failure so much as an insertion point: <code>v.insert(i, x)</code> keeps the Vec sorted.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![10, 20, 30, 40];<br>    println!(&quot;{:?}&quot;, v.binary_search(&amp;30));<br>    println!(&quot;{:?}&quot;, v.binary_search(&amp;35));<br><br>    let mut w = vec![10, 20, 40];<br>    let at = w.binary_search(&amp;30).unwrap_or_else(|i| i);<br>    w.insert(at, 30);<br>    println!(&quot;{w:?}&quot;);<br>}<br><br>// Ok(2)<br>// Err(3)<br>// [10, 20, 30, 40]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>READ TABLE itab ... BINARY SEARCH</code> &mdash; same precondition, same silent-wrong-answer failure mode if the table is not sorted by that key.<br><b>Python:</b> <code>bisect.bisect_left</code> gives you the <code>Err(i)</code> half.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec result abap gotcha
You are writing a function that reads a list of numbers. Should the parameter be <code>&amp;Vec&lt;i32&gt;</code> or <code>&amp;[i32]</code>?	<b><code>&amp;[i32]</code>. Always, for a read-only parameter.</b><br><br><code>Vec&lt;T&gt;</code> derefs to <code>[T]</code>, so a <code>&amp;Vec&lt;i32&gt;</code> coerces at the call site for free &mdash; existing callers do not change. And <code>&amp;[i32]</code> <i>additionally</i> accepts arrays, sub-slices (<code>&amp;v[1..3]</code>), and anything else that derefs to a slice.<br><br>Same reasoning as <code>&amp;str</code> over <code>&amp;String</code>. It is the same rule.<br><br>That deref is also why <code>first</code>, <code>last</code>, <code>contains</code>, <code>sort</code>, <code>windows</code>, <code>iter</code> are not on <code>Vec</code> at all &mdash; they are slice methods you get free.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn total(xs: &amp;[i32]) -&gt; i32 { xs.iter().sum() }<br><br>fn main() {<br>    let v = vec![1, 2, 3, 4];<br>    println!(&quot;{}&quot;, total(&amp;v));<br>    println!(&quot;{}&quot;, total(&amp;v[1..3]));<br>    println!(&quot;{}&quot;, total(&amp;[10, 20]));<br>}<br><br>// 10<br>// 5<br>// 30</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/arrays_and_slices/index.html">arrays_and_slices</a></div>	rust vec api-design
<code>.collect()</code> needs to know what to build. Two ways to tell it &mdash; write both.	<b>Annotate the binding: <code>let v: Vec&lt;i32&gt; = it.collect();</code><br>or turbofish the call: <code>it.collect::&lt;Vec&lt;i32&gt;&gt;()</code></b><br><br>Without one of them: <code>E0282, type annotations needed</code>. <code>collect</code> is generic over its <i>return</i> type, which inference cannot guess.<br><br><code>Vec&lt;_&gt;</code> is usually enough &mdash; the element type is inferable, only the container is not.<br><br>It builds more than Vecs: <code>String</code>, <code>HashMap</code>, <code>HashSet</code>, and &mdash; the good trick &mdash; <code>Result&lt;Vec&lt;T&gt;, E&gt;</code> from an iterator of Results, which short-circuits on the first error.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v: Vec&lt;u32&gt; = (1..=3).collect();<br>    let w = (1..=3).map(|n| n * n).collect::&lt;Vec&lt;_&gt;&gt;();<br>    println!(&quot;{v:?} {w:?}&quot;);<br><br>    let nums: Result&lt;Vec&lt;i32&gt;, _&gt; = [&quot;1&quot;, &quot;2&quot;, &quot;x&quot;].iter().map(|s| s.parse::&lt;i32&gt;()).collect();<br>    println!(&quot;{}&quot;, nums.is_err());<br>}<br><br>// [1, 2, 3] [1, 4, 9]<br>// true</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/collect_into_a_vec/index.html">collect_into_a_vec</a></div>	rust vec iterators
Why does <code>v.iter().sum()</code> sometimes fail to compile, and how do you fix it?	<b><code>sum</code> is generic over its output type. Annotate it: <code>let t: i32 = v.iter().sum();</code> or <code>v.iter().sum::&lt;i32&gt;()</code>.</b><br><br>Same shape of problem as <code>collect</code>. It works unannotated only when the surrounding code already pins the type.<br><br>The same applies to <code>product()</code>. Not to <code>max()</code>/<code>min()</code>, which return <code>Option&lt;&amp;T&gt;</code> with the element type already known.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    let t: i32 = v.iter().sum();<br>    println!(&quot;{t} {}&quot;, v.iter().sum::&lt;i32&gt;());<br>    println!(&quot;{:?} {:?}&quot;, v.iter().max(), v.iter().min());<br>    println!(&quot;{}&quot;, v.iter().product::&lt;i32&gt;());<br>}<br><br>// 6 6<br>// Some(3) Some(1)<br>// 6</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/index.html">iterators</a></div>	rust vec iterators
You need the <b>index</b> of an element while looping, and separately the index of the first match. Which two methods?	<b><code>.iter().enumerate()</code> &rarr; <code>(usize, &amp;T)</code><br><code>.iter().position(|x| ...)</code> &rarr; <code>Option&lt;usize&gt;</code></b><br><br><code>position</code> short-circuits on the first hit. <code>rposition</code> searches from the end. <code>find</code> is the sibling that returns the <i>element</i> instead of the index.<br><br>Note <code>position</code>'s closure takes <code>&amp;&amp;T</code> from <code>iter()</code> &mdash; hence the <code>|&amp;x|</code> pattern, or compare with <code>*x</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![&quot;a&quot;, &quot;b&quot;, &quot;c&quot;];<br>    for (i, s) in v.iter().enumerate() {<br>        print!(&quot;{i}:{s} &quot;);<br>    }<br>    println!();<br>    println!(&quot;{:?}&quot;, v.iter().position(|&amp;s| s == &quot;b&quot;));<br>    println!(&quot;{:?}&quot;, v.iter().position(|&amp;s| s == &quot;z&quot;));<br>    println!(&quot;{:?}&quot;, v.iter().find(|s| s.starts_with(&#x27;c&#x27;)));<br>}<br><br>// 0:a 1:b 2:c <br>// Some(1)<br>// None<br>// Some(&quot;c&quot;)</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>enumerate(lst)</code> is identical; <code>lst.index(x)</code> raises when missing, where <code>position</code> returns <code>None</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/index.html">iterators</a></div>	rust vec iterators option
Test whether a Vec contains a value. Write it &mdash; and watch the argument.	<b><code>v.contains(&amp;x)</code> &mdash; it takes a REFERENCE.</b><br><br><code>v.contains(x)</code> will not compile: the signature is <code>contains(&amp;self, x: &amp;T)</code>.<br><br>Careful on <code>Vec&lt;String&gt;</code>: <code>v.contains(&amp;"a")</code> fails, because <code>&amp;&amp;str</code> is not <code>&amp;String</code>. Use <code>v.iter().any(|s| s == "a")</code>, which compares through deref.<br><br>And note it is <b>O(n)</b>. If you do this in a loop, you want a <code>HashSet</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    println!(&quot;{}&quot;, v.contains(&amp;2));<br><br>    let names = vec![String::from(&quot;ada&quot;)];<br>    println!(&quot;{}&quot;, names.iter().any(|s| s == &quot;ada&quot;));<br>}<br><br>// true<br>// true</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>line_exists( itab[ key = x ] )</code>. <b>Python:</b> <code>x in lst</code> &mdash; also O(n), and also the signal to reach for a set.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_hashset/index.html">the_hashset</a></div>	rust vec gotcha
Add all of <code>b</code>'s elements to <code>a</code>. Three ways &mdash; and what happens to <code>b</code> in each?	<b><code>a.extend(b)</code> &mdash; consumes <code>b</code><br><code>a.append(&amp;mut b)</code> &mdash; <b>EMPTIES</b> <code>b</code>, which stays alive<br><code>a.extend_from_slice(&amp;b)</code> &mdash; clones; <code>b</code> untouched</b><br><br><code>append</code> is the surprising one: <code>b</code> is still there afterwards, but with length 0. It is the fastest, because elements are moved in bulk with no per-item work.<br><br><code>extend</code> takes any <code>IntoIterator</code>, so <code>a.extend(1..=3)</code> works too.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut a = vec![1, 2];<br>    let mut b = vec![3, 4];<br>    a.append(&amp;mut b);<br>    println!(&quot;{a:?} {b:?}&quot;);<br><br>    let c = vec![5, 6];<br>    a.extend_from_slice(&amp;c);<br>    a.extend(7..=8);<br>    println!(&quot;{a:?} {c:?}&quot;);<br>}<br><br>// [1, 2, 3, 4] []<br>// [1, 2, 3, 4, 5, 6, 7, 8] [5, 6]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>a.extend(b)</code> leaves <code>b</code> alone &mdash; that is Rust's <code>extend_from_slice</code>, not its <code>append</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec gotcha
Take a range of elements OUT of a Vec and use them, leaving the Vec shorter. Which method?	<b><code>v.drain(1..3)</code> &mdash; yields the removed elements as an iterator and removes them from the Vec.</b><br><br><code>v.drain(..)</code> empties it while handing you everything &mdash; the way to reuse an allocation without cloning.<br><br>The removal happens even if you drop the iterator without reading it. <code>v.clear()</code> is <code>drain(..)</code> with the values thrown away.<br><br>Compare: <code>split_off(n)</code> gives you the tail as a NEW Vec.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3, 4, 5];<br>    let taken: Vec&lt;i32&gt; = v.drain(1..3).collect();<br>    println!(&quot;{taken:?} {v:?}&quot;);<br><br>    let mut w = vec![1, 2, 3, 4];<br>    let tail = w.split_off(2);<br>    println!(&quot;{w:?} {tail:?}&quot;);<br>}<br><br>// [2, 3] [1, 4, 5]<br>// [1, 2] [3, 4]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec
After <code>v.clear()</code>, what are <code>len</code> and <code>capacity</code>?	<b><code>len</code> is 0. <code>capacity</code> is UNCHANGED &mdash; the buffer is kept.</b><br><br>That is a feature: clearing and refilling in a loop reuses one allocation. Same for <code>truncate(n)</code>, which only drops the tail.<br><br>Actually give the memory back with <code>shrink_to_fit()</code>, or drop the Vec. <code>v = Vec::new()</code> also does it &mdash; the old buffer is freed with the old value.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3, 4, 5];<br>    v.truncate(2);<br>    println!(&quot;{v:?} len {} cap {}&quot;, v.len(), v.capacity());<br>    v.clear();<br>    println!(&quot;len {} cap {}&quot;, v.len(), v.capacity());<br>    v.shrink_to_fit();<br>    println!(&quot;cap {}&quot;, v.capacity());<br>}<br><br>// [1, 2] len 2 cap 5<br>// len 0 cap 5<br>// cap 0</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec memory
Copying a <code>Vec&lt;String&gt;</code> into one you already have. Which does the LEAST new allocation?<br><br><code>dst = src.clone()</code> &mdash; <code>std::clone::Clone</code><br><code>dst.clone_from(&amp;src)</code> &mdash; <code>std::clone::Clone</code><br><code>src.clone_into(&amp;mut dst)</code> &mdash; <code>std::borrow::ToOwned</code>	<b>The last two tie &mdash; they are the same function. <code>clone()</code> is the only one that always pays.</b><br><br>Counted with a counting global allocator, four names into a destination that already holds four roomy slots: <code>clone()</code> costs <b>5</b> allocations (the <code>Vec</code>, then one per <code>String</code>); <code>clone_from</code> and <code>clone_into</code> cost <b>0</b> each.<br><br>They tie because <code>Vec::clone_from</code> IS <code>SpecCloneIntoVec::clone_into(source.as_slice(), self)</code>, and the blanket <code>impl&lt;T: Clone&gt; ToOwned for T</code> defines <code>clone_into</code> as <code>target.clone_from(self)</code>. One body, two spellings &mdash; pointing opposite ways, since <code>clone_into</code> is called on the SOURCE.<br><br>Zero rather than one: the overlapping prefix goes through <code>clone_from_slice</code>, so the inner <code>String</code> buffers are refilled in place too, not just the <code>Vec</code>&rsquo;s.<br><br><b>&quot;Already have&quot; is the whole condition.</b> Into an EMPTY <code>Vec</code> all three allocate 5 &mdash; there is nothing to reuse. Roomy slots holding 4-byte <code>String</code>s: 0 allocations and 4 reallocations. The saving is real only from the second call onward, which is why the pattern is a buffer hoisted OUT of a loop.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let src = vec![String::from(&quot;Ada&quot;), String::from(&quot;Ben&quot;)];<br>    let mut dst = vec![String::with_capacity(8), String::with_capacity(8)];<br>    let (vec_buf, str_buf) = (dst.as_ptr(), dst[0].as_ptr());<br><br>    dst.clone_from(&amp;src);              // same call as src.clone_into(&amp;mut dst)<br>    println!(&quot;{} {}&quot;, dst.as_ptr() == vec_buf, dst[0].as_ptr() == str_buf);<br><br>    let fresh = src.clone();<br>    println!(&quot;{}&quot;, fresh.as_ptr() == vec_buf);<br>}<br><br>// true true<br>// false</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>lt_target = lt_source</code> deep-copies the table; the reusing spelling is <code>CLEAR lt_target. APPEND LINES OF lt_source TO lt_target.</code> &mdash; and ABAP lets you forget, where Rust makes you name which one you meant.<br><b>Python:</b> <code>dst[:] = src</code> keeps the list object, <code>dst = list(src)</code> builds a new one. But Python&rsquo;s elements are references, so nothing there corresponds to refilling the inner buffers.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/12_Traits/clone_into/index.html">clone_into</a></div>	rust vec memory performance
Make a 2-row &times; 3-column grid of zeros, then set row 1 column 2 to 9.	<b><code>let mut g = vec![vec![0; 3]; 2];</code> then <code>g[1][2] = 9;</code></b><br><br>The inner <code>vec!</code> is cloned per row, so each row is its own allocation &mdash; <i>n</i>+1 allocations for <i>n</i> rows, and the rows are scattered in memory.<br><br>For anything performance-sensitive, use <b>one flat Vec</b> and index arithmetic: <code>g[row * cols + col]</code>. One allocation, cache-friendly, and the shape lives in two <code>usize</code>s you keep beside it.<br><br>Watch out: <code>vec![vec![0; 3]; 2]</code> clones (fine), but with <code>Rc</code>/<code>RefCell</code> inside, cloning shares &mdash; a classic accidental-aliasing bug.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut g = vec![vec![0; 3]; 2];<br>    g[1][2] = 9;<br>    println!(&quot;{g:?}&quot;);<br><br>    let (rows, cols) = (2, 3);<br>    let mut flat = vec![0; rows * cols];<br>    flat[1 * cols + 2] = 9;<br>    println!(&quot;{flat:?}&quot;);<br>}<br><br>// [[0, 0, 0], [0, 0, 9]]<br>// [0, 0, 0, 0, 0, 9]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/vec_of_vecs/index.html">vec_of_vecs</a></div>	rust vec
Walk a Vec in overlapping pairs, and separately in fixed-size batches. Which two methods?	<b><code>v.windows(2)</code> &mdash; OVERLAPPING, sliding by one<br><code>v.chunks(2)</code> &mdash; DISJOINT batches, last one may be short</b><br><br><code>windows</code> is the tool for "compare each element to the next" &mdash; differences, is-sorted checks, run detection. It yields nothing at all if the slice is shorter than the window.<br><br><code>chunks_exact(n)</code> drops the short tail (and is faster); <code>chunks_mut</code> lets you write.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 4, 8, 9];<br>    println!(&quot;{:?}&quot;, v.windows(2).collect::&lt;Vec&lt;_&gt;&gt;());<br>    println!(&quot;{:?}&quot;, v.chunks(2).collect::&lt;Vec&lt;_&gt;&gt;());<br>    let deltas: Vec&lt;i32&gt; = v.windows(2).map(|w| w[1] - w[0]).collect();<br>    println!(&quot;{deltas:?}&quot;);<br>    println!(&quot;{}&quot;, v.windows(2).all(|w| w[0] &lt;= w[1]));<br>}<br><br>// [[1, 2], [2, 4], [4, 8], [8, 9]]<br>// [[1, 2], [4, 8], [9]]<br>// [1, 2, 4, 1]<br>// true</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/arrays_and_slices/index.html">arrays_and_slices</a></div>	rust vec iterators
Double every element. Two idioms &mdash; when do you want each?	<b>New Vec: <code>let d: Vec&lt;i32&gt; = v.iter().map(|x| x * 2).collect();</code><br>In place: <code>for x in &amp;mut v { *x *= 2; }</code></b><br><br><code>map</code>+<code>collect</code> allocates a second Vec and can change the element type. <code>iter_mut</code> allocates nothing and cannot.<br><br>Reach for <code>map</code> by default &mdash; it composes with <code>filter</code>, <code>enumerate</code>, <code>take</code>, and it reads as a pipeline. Reach for <code>iter_mut</code> when the Vec is large and the type is unchanged.<br><br>Don't forget the <code>*</code>: <code>x</code> is a <code>&amp;mut i32</code>, not an <code>i32</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    let doubled: Vec&lt;i32&gt; = v.iter().map(|x| x * 2).collect();<br>    let labels: Vec&lt;String&gt; = v.iter().map(|x| format!(&quot;#{x}&quot;)).collect();<br>    println!(&quot;{doubled:?} {labels:?}&quot;);<br><br>    let mut w = vec![1, 2, 3];<br>    for x in &amp;mut w { *x *= 2; }<br>    println!(&quot;{w:?}&quot;);<br><br>    let evens: Vec&lt;i32&gt; = (1..=10).filter(|n| n % 2 == 0).collect();<br>    println!(&quot;{evens:?}&quot;);<br>}<br><br>// [2, 4, 6] [&quot;#1&quot;, &quot;#2&quot;, &quot;#3&quot;]<br>// [2, 4, 6]<br>// [2, 4, 6, 8, 10]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>[x * 2 for x in v]</code> is the <code>map</code>+<code>collect</code> line; Python has no clean in-place equivalent.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/index.html">iterators</a></div>	rust vec iterators
Turn <code>Vec&lt;String&gt;</code> into one comma-separated <code>String</code>. And <code>Vec&lt;Vec&lt;i32&gt;&gt;</code> into a flat <code>Vec&lt;i32&gt;</code>?	<b><code>v.join(", ")</code> for the strings.<br><code>v.concat()</code> or <code>v.into_iter().flatten().collect()</code> for the nested Vec.</b><br><br>Both live on the <b>slice</b>, which is why they work on a <code>Vec</code> at all. <code>concat()</code> is <code>join</code> with no separator.<br><br>For non-string elements, go through <code>map</code> first: <code>v.iter().map(|n| n.to_string()).collect::&lt;Vec&lt;_&gt;&gt;().join(", ")</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let names = vec![String::from(&quot;ada&quot;), String::from(&quot;bob&quot;)];<br>    println!(&quot;{}&quot;, names.join(&quot;, &quot;));<br><br>    let nested = vec![vec![1, 2], vec![3]];<br>    println!(&quot;{:?}&quot;, nested.concat());<br><br>    let nums = vec![1, 2, 3];<br>    println!(&quot;{}&quot;, nums.iter().map(|n| n.to_string()).collect::&lt;Vec&lt;_&gt;&gt;().join(&quot;-&quot;));<br>}<br><br>// ada, bob<br>// [1, 2, 3]<br>// 1-2-3</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>CONCATENATE LINES OF itab INTO lv SEPARATED BY ', '</code>.<br><b>Python:</b> <code>", ".join(names)</code> &mdash; separator first, the other way round from Rust.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec strings
<code>v.reverse()</code> vs <code>v.iter().rev()</code> &mdash; what is the difference?	<b><code>reverse()</code> mutates the Vec in place and returns <code>()</code>. <code>rev()</code> walks it backwards and changes nothing.</b><br><br><code>let r = v.reverse();</code> is the classic slip: <code>r</code> is <code>()</code>. Any Rust method named as an imperative verb (<code>sort</code>, <code>reverse</code>, <code>push</code>, <code>clear</code>, <code>truncate</code>) mutates and returns nothing.<br><br><code>rev()</code> needs a <code>DoubleEndedIterator</code> &mdash; fine for slices and Vecs, not for a <code>HashMap</code>.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![1, 2, 3];<br>    v.reverse();<br>    println!(&quot;{v:?}&quot;);<br><br>    let w = vec![1, 2, 3];<br>    println!(&quot;{:?} {w:?}&quot;, w.iter().rev().collect::&lt;Vec&lt;_&gt;&gt;());<br>}<br><br>// [3, 2, 1]<br>// [3, 2, 1] [1, 2, 3]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> exactly the same split &mdash; <code>lst.reverse()</code> returns <code>None</code> and mutates; <code>reversed(lst)</code> and <code>lst[::-1]</code> do not.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec gotcha
<code>insert(i, x)</code> and <code>swap(i, j)</code> &mdash; what do they cost?	<b><code>insert(i, x)</code> is O(n) &mdash; it shifts everything from <code>i</code> rightwards. <code>swap(i, j)</code> is O(1).</b><br><br><code>insert(0, x)</code> &mdash; prepending &mdash; copies the whole Vec every time. In a loop that is O(n&sup2;). If you need to prepend often you want a <code>VecDeque</code>, which has <code>push_front</code>.<br><br>Both panic if the index is out of range (<code>insert</code> allows <code>i == len</code>, which is a push).<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let mut v = vec![&#x27;a&#x27;, &#x27;c&#x27;];<br>    v.insert(1, &#x27;b&#x27;);<br>    v.insert(3, &#x27;d&#x27;);<br>    println!(&quot;{v:?}&quot;);<br>    v.swap(0, 3);<br>    println!(&quot;{v:?}&quot;);<br>}<br><br>// [&#x27;a&#x27;, &#x27;b&#x27;, &#x27;c&#x27;, &#x27;d&#x27;]<br>// [&#x27;d&#x27;, &#x27;b&#x27;, &#x27;c&#x27;, &#x27;a&#x27;]</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>INSERT wa INTO itab INDEX i</code> &mdash; same shift, same cost.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec performance
Ask "does any element match?" and "do all of them?" &mdash; and what do these do on an EMPTY Vec?	<b><code>v.iter().any(|x| ...)</code> and <code>v.iter().all(|x| ...)</code> &mdash; both return <code>bool</code> and both short-circuit.</b><br><br>On an empty Vec: <code>any</code> is <b>false</b>, <code>all</code> is <b>true</b>. That is the mathematical convention (vacuous truth), it is almost never what a first-time reader expects, and it is a real source of bugs in validation code &mdash; "all rows are valid" is true when there are no rows.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![2, 4, 6];<br>    println!(&quot;{} {}&quot;, v.iter().any(|n| n % 2 == 1), v.iter().all(|n| n % 2 == 0));<br><br>    let empty: Vec&lt;i32&gt; = vec![];<br>    println!(&quot;{} {}&quot;, empty.iter().any(|n| *n &gt; 0), empty.iter().all(|n| *n &gt; 0));<br>}<br><br>// false true<br>// false true</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>Python:</b> <code>any([])</code> is <code>False</code>, <code>all([])</code> is <code>True</code> &mdash; identical convention, identical trap.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/24_Iterators/index.html">iterators</a></div>	rust vec iterators gotcha
<code>len()</code>, <code>is_empty()</code>, and iterating a Vec while you need its length &mdash; any traps?	<b><code>v.len()</code> is O(1) (it is one of the three stored numbers). <code>v.is_empty()</code> is the idiomatic emptiness test.</b><br><br>The trap is the loop: <code>for i in 0..v.len()</code> reads the length ONCE, before the loop body ever runs. If the body pushes, the loop will not see the new elements; if it removes, the index will go out of range and panic.<br><br>That is a good thing &mdash; and the reason to write <code>for x in &amp;v</code>, which the borrow checker will not let you invalidate at all.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">fn main() {<br>    let v = vec![1, 2, 3];<br>    println!(&quot;{} {}&quot;, v.len(), v.is_empty());<br>    let e: Vec&lt;i32&gt; = Vec::new();<br>    println!(&quot;{} {}&quot;, e.len(), e.is_empty());<br>}<br><br>// 3 false<br>// 0 true</pre><br><div style="opacity:.85; font-size:.95em; border-top:1px solid #9995; margin-top:12px; padding-top:8px;"><b>ABAP:</b> <code>lines( itab )</code> and <code>itab IS INITIAL</code>.</div><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/the_vec/index.html">the_vec</a></div>	rust vec
When is <code>Vec</code> the wrong choice? Name the three alternatives and their trigger.	<b>• repeated <code>contains</code> / membership &rarr; <code>HashSet</code> (O(1) instead of O(n))<br>• lookup by key &rarr; <code>HashMap</code><br>• pushing or popping at the FRONT &rarr; <code>VecDeque</code> (<code>push_front</code> is O(1))</b><br><br>Also: a fixed, compile-time-known size &rarr; an array <code>[T; N]</code>, which lives on the stack with no allocation at all.<br><br>And the one that is <i>not</i> a reason to leave: needing a sorted list. <code>Vec</code> + <code>sort</code> + <code>binary_search</code> beats a tree for most real sizes, because it is one contiguous block and the cache loves it.<br><div style="opacity:.6; font-size:.8em; margin-top:8px;">verified — rustc 1.98.0, edition 2024</div><pre style="text-align:left; white-space:pre-wrap; font-size:0.92em; line-height:1.45; border-left:3px solid #7a7a7a; padding:2px 0 2px 10px; margin:10px 0; font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;">use std::collections::{HashSet, VecDeque};<br><br>fn main() {<br>    let seen: HashSet&lt;i32&gt; = vec![1, 2, 2, 3].into_iter().collect();<br>    println!(&quot;{} {}&quot;, seen.len(), seen.contains(&amp;2));<br><br>    let mut q: VecDeque&lt;i32&gt; = VecDeque::new();<br>    q.push_back(2);<br>    q.push_front(1);<br>    println!(&quot;{:?} {:?}&quot;, q.pop_front(), q);<br>}<br><br>// 3 true<br>// Some(1) [2]</pre><br><div style="margin-top:10px; font-size:.9em;">&rarr; <a href="https://masiarek.github.io/rust-learning-library/26_Collections/index.html">collections</a></div>	rust vec collections
