# `str::to_ascii_uppercase`

**Level:** reference · for working programmers

**One line:** A new `String` with `a`–`z` uppercased and everything else untouched — length-preserving, unlike [`to_uppercase`](../str_to_uppercase/README.md).

```text
pub fn to_ascii_uppercase(&self) -> String
```

Stable since **1.23.0**.

The mirror of [`to_ascii_lowercase`](../str_to_ascii_lowercase/README.md), and it avoids the `ß` → `SS` expansion entirely by simply not touching `ß`.

That makes it round-trippable within ASCII: `to_ascii_uppercase().to_ascii_lowercase()` returns the original for any ASCII input, which the Unicode pair does not guarantee.

Use it for protocol tokens, hex digits, and anything specified in ASCII. Use `to_uppercase` for text a person will read.

## Example

<!-- source:str_to_ascii_uppercase -->
*[`str_to_ascii_uppercase.rs`](examples/str_to_ascii_uppercase.rs) in full — pasted here by `tools/run_examples.py` from the file CI compiles and runs.*

```rust
fn main() {
    println!("{:?}", "hello".to_ascii_uppercase());

    // ß is left alone, so no expansion.
    println!("{:?} vs {:?}", "straße".to_ascii_uppercase(), "straße".to_uppercase());

    // Round-trips within ASCII.
    let s = "MiXeD case 123!";
    println!("{}", s.to_ascii_uppercase().to_ascii_lowercase() == s.to_ascii_lowercase());

    // Length is always preserved.
    for s in ["ß", "ﬁ", "abc"] {
        println!("{:<5} ascii {} bytes, unicode {} bytes",
                 format!("{s:?}"), s.to_ascii_uppercase().len(), s.to_uppercase().len());
    }

    // Hex digits: an ASCII-by-definition alphabet.
    println!("{:?}", "1f4a9".to_ascii_uppercase());
}
```
<!-- /source -->

<!-- output:str_to_ascii_uppercase -->
*Verified output of [`str_to_ascii_uppercase.rs`](examples/str_to_ascii_uppercase.rs) — regenerated by `tools/run_examples.py`, never hand-typed.*

```text
"HELLO"
"STRAßE" vs "STRASSE"
true
"ß"   ascii 2 bytes, unicode 2 bytes
"ﬁ"   ascii 3 bytes, unicode 2 bytes
"abc" ascii 3 bytes, unicode 3 bytes
"1F4A9"
```
<!-- /output -->

## See also

- [`str::to_ascii_lowercase`](../str_to_ascii_lowercase/README.md) — the other direction
- [`str::to_uppercase`](../str_to_uppercase/README.md) — the Unicode-aware version
- [`str::make_ascii_uppercase`](../str_make_ascii_uppercase/README.md) — the same edit, in place
- [`str::is_ascii`](../str_is_ascii/README.md) — checking the assumption

[`str::to_ascii_uppercase` in the standard library ↗](https://doc.rust-lang.org/std/primitive.str.html#method.to_ascii_uppercase)
