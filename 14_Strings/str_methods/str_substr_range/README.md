# `str::substr_range`

**Level:** reference · for working programmers

**One line:** The byte range a sub-slice occupies **within this string**, or `None` if it is not part of it — an identity question, not a search.

```text
pub fn substr_range(&self, substr: &str) -> Option<Range<usize>>
```

Stable since **1.98.0**.

This does not look for matching text. It compares *addresses*: it answers "is this `&str` a view into that `&str`, and if so, where". A slice with identical contents taken from a different allocation gives `None`.

That is the difference from [`find`](../str_find/README.md), which searches by content and would happily report an offset for an unrelated string that happens to match.

It is the safe replacement for the pointer arithmetic people write with [`as_ptr`](../str_as_ptr/README.md): subtracting two addresses to recover an offset does the same job with none of the guarantees.

The realistic use is in parsers and tokenizers. Once a lexer has produced a token as a `&str` borrowed from the input, this recovers its position for an error message — without threading offsets through every function.

## Example

<!-- source:str_substr_range -->
*[`str_substr_range.rs`](examples/str_substr_range.rs) in full — pasted here by `tools/run_examples.py` from the file CI compiles and runs.*

```rust
fn main() {
    let text = "let x = 42;";

    let token = &text[8..10];
    println!("{token:?} at {:?}", text.substr_range(token));

    // Identity, not content: an equal string from elsewhere is not part of it.
    let elsewhere = String::from("42");
    println!("{:?}", text.substr_range(elsewhere.as_str()));
    println!("but find() matches by content: {:?}", text.find("42"));

    // Recovering a token's position for an error message.
    for tok in text.split_whitespace() {
        let at = text.substr_range(tok).map(|r| r.start);
        println!("{:<5} column {:?}", format!("{tok:?}"), at);
    }

    // The whole string is a substring of itself.
    println!("{:?}", text.substr_range(text));
}
```
<!-- /source -->

<!-- output:str_substr_range -->
*Verified output of [`str_substr_range.rs`](examples/str_substr_range.rs) — regenerated by `tools/run_examples.py`, never hand-typed.*

```text
"42" at Some(8..10)
None
but find() matches by content: Some(8)
"let" column Some(0)
"x"   column Some(4)
"="   column Some(6)
"42;" column Some(8)
Some(0..11)
```
<!-- /output -->

## See also

- [`str::find`](../str_find/README.md) — searching by content instead of identity
- [`str::as_ptr`](../str_as_ptr/README.md) — the unsafe pointer arithmetic this replaces
- [`str::char_indices`](../str_char_indices/README.md) — offsets while iterating rather than afterwards
- [`str::split_whitespace`](../str_split_whitespace/README.md) — a common source of the borrowed tokens

[`str::substr_range` in the standard library ↗](https://doc.rust-lang.org/std/primitive.str.html#method.substr_range)
