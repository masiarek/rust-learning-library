# `str::as_mut_ptr`

**Level:** reference · for working programmers

**One line:** The raw address of the first byte as `*mut u8`, from a `&mut str` — the writable counterpart of [`as_ptr`](../str_as_ptr/README.md).

```text
pub const fn as_mut_ptr(&mut self) -> *mut u8
```

Stable since **1.36.0**. Usable in a `const` context.

Same pointer, with permission to write through it. That permission is the whole difficulty: **`str` is not just bytes, it is bytes that are valid UTF-8**, and writing through this pointer can break that. A `&str` whose bytes are not valid UTF-8 is undefined behaviour, not merely a wrong answer — every other method is entitled to assume the invariant holds.

Writing an ASCII byte over another ASCII byte is the one edit that is trivially safe, because every ASCII byte is a complete character. That is exactly what [`make_ascii_uppercase`](../str_make_ascii_uppercase/README.md) does, safely, and it is almost always the method you actually wanted.

Getting a `&mut str` at all is unusual — string literals are immutable, so it comes from [`String::as_mut_str`](../../string_methods/string_as_mut_str/README.md) or a `&mut String` coercion.

## Example

<!-- source:str_as_mut_ptr -->
*[`str_as_mut_ptr.rs`](examples/str_as_mut_ptr.rs) in full — pasted here by `tools/run_examples.py` from the file CI compiles and runs.*

```rust
fn main() {
    let mut owned = String::from("hello");

    // Safe, because ASCII -> ASCII is byte-for-byte and cannot break UTF-8.
    let s: &mut str = owned.as_mut_str();
    let p = s.as_mut_ptr();
    unsafe { *p = b'H'; }
    println!("{owned}");

    // The safe spelling of the same edit.
    let mut safe = String::from("hello");
    safe.as_mut_str().make_ascii_uppercase();
    println!("{safe}");

    // Why it is unsafe: 'é' is two bytes, so overwriting one of them alone
    // would leave the string invalid. Shown as a comment, not run.
    let two_byte = String::from("é");
    println!("'é' occupies {} bytes", two_byte.len());
}
```
<!-- /source -->

<!-- output:str_as_mut_ptr -->
*Verified output of [`str_as_mut_ptr.rs`](examples/str_as_mut_ptr.rs) — regenerated by `tools/run_examples.py`, never hand-typed.*

```text
Hello
HELLO
'é' occupies 2 bytes
```
<!-- /output -->

## See also

- [`str::as_ptr`](../str_as_ptr/README.md) — the read-only counterpart
- [`str::as_bytes_mut`](../str_as_bytes_mut/README.md) — the same permission, as a slice
- [`str::make_ascii_uppercase`](../str_make_ascii_uppercase/README.md) — the safe in-place edit this is usually reaching for
- [`String::as_mut_str`](../../string_methods/string_as_mut_str/README.md) — where a `&mut str` comes from

[`str::as_mut_ptr` in the standard library ↗](https://doc.rust-lang.org/std/primitive.str.html#method.as_mut_ptr)
