# `str::trim_right_matches`

**Level:** reference · for working programmers

> **Deprecated since Rust 1.33 — use [`str::trim_end_matches`](../str_trim_end_matches/README.md) instead.** The page is here because the name is still in code written before it moved, and it still compiles; a new call site should not use it.

**One line:** The pre-1.33 name for [`trim_end_matches`](../str_trim_end_matches/README.md).

```text
pub fn trim_right_matches<P: Pattern>(&self, pat: P) -> &str
where
    for<'a> P::Searcher<'a>: ReverseSearcher<'a>,
```



Same rename, same reason. Behaviour is unchanged: the pattern is removed from the back repeatedly.

## Example

<!-- source:str_trim_right_matches -->
*[`str_trim_right_matches.rs`](examples/str_trim_right_matches.rs) in full — pasted here by `tools/run_examples.py` from the file CI compiles and runs.*

```rust
#![allow(deprecated)]

fn main() {
    let s = "value///";
    println!("{:?}", s.trim_right_matches('/'));
    println!("{:?}", s.trim_end_matches('/'));
    println!("identical: {}", s.trim_right_matches('/') == s.trim_end_matches('/'));
}
```
<!-- /source -->

<!-- output:str_trim_right_matches -->
*Verified output of [`str_trim_right_matches.rs`](examples/str_trim_right_matches.rs) — regenerated by `tools/run_examples.py`, never hand-typed.*

```text
"value"
"value"
identical: true
```
<!-- /output -->

## See also

- [`str::trim_end_matches`](../str_trim_end_matches/README.md) — the method to use
- [`str::strip_suffix`](../str_strip_suffix/README.md) — one occurrence, reported
- [`str::trim_left_matches`](../str_trim_left_matches/README.md) — the other deprecated name

[`str::trim_right_matches` in the standard library ↗](https://doc.rust-lang.org/std/primitive.str.html#method.trim_right_matches)
