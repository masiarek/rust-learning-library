# When the denominators compound

**Level:** 301 · deep dive

**One line:** The companion trick works because one method's denominators can be listed before the count starts; this page is the method where they cannot, and the useful part is not that the technique fails but *how* it fails — the width you need is a property of the ballots, not of the election, so you cannot pick an integer type in advance and the only honest design is one that detects rather than assumes.

[Scale the denominator away](../scaled_integers/README.md) ends on a test: *is this round's weight a function of an integer, or of last round's weight?* Reweighted Range Voting is the first kind, and the whole count collapses into `i128`. This page takes the second kind seriously, because "the trick doesn't apply here" is where most write-ups stop and it is the least useful sentence available.

---

## The one line of difference

Both methods are proportional score counts. Both shrink a ballot's weight after each seat so that voters who already elected someone have less say in the next seat. They differ in one statement:

```rust
// Reweighted Range Voting — weight is a function of an integer
weight = C / (C + spent);                    // `spent` is a running integer total

// Allocated Score — weight is a function of the weights
weight = weight * (1 - quota / allocated);   // `allocated` is a sum of current weights
```

In the first, a ballot's weight at seat 7 depends on nothing but a small integer, so every denominator that can ever occur is `C..=(C + seats*C)` and you can take their lcm before reading a ballot. In the second, this round's denominator is *built out of* last round's, through a sum over whichever ballots happened to tie at the top score. There is no set to enumerate, because the set is not determined until the votes are.

Here is that happening on a real election — BetterVoting's own `AllocatedScore` test, twelve ballots and two seats:

<!-- output:compounding_weights -->
*Verified output of [`compounding_weights.rs`](examples/compounding_weights.rs) — regenerated by `tools/run_examples.py`, never hand-typed.*

```text
1. Where the denominator comes from
   Twelve ballots, two seats, so the Hare quota is 12/2 = 6 ballots.
   Allison wins seat 1 with 8 supporters at the top score — 8 units of
   weight to spend a 6-unit quota, so each keeps 1 - 6/8:

     seat 1 -> candidate 0   widest weight 1/4   (3 bits)

   winners: Allison, Doug

   The 4 in that 1/4 was not chosen from any fixed set. It is the total
   weight of whoever happened to tie at the top score — so seat 3's
   denominators would be built from seat 2's weights, and so on down.
   There is no set of denominators to take an lcm over.

2. Eight electorates of exactly the same size
   900 ballots, 18 candidates, 12 seats. Only the votes differ.

     widest denominator, in bits: [31, 31, 32, 33, 39, 33, 35, 27]
     min 27, max 39, spread 12 bits
   Same ballot count, same seats, same candidates. The arithmetic the
   count needs is decided by how people voted, and by nothing you know
   before they vote.

3. The same election, counted two ways
   2000 ballots, 30 candidates, 20 seats. Both additions are exact;
   they differ only in how big a number they build on the way.

     a/b + c/d = (ad+cb)/bd  -> OVERFLOWED at seat 16 (widest so far 44 bits)
     both over lcm(b, d)     -> all 20 seats, widest denominator 68 bits

   Read those two lines together. One counted the whole election; the
   other gave up four seats short. And the widest number the finishing
   count ever had to REPRESENT was 68 bits — barely half of i128's 127.
   The failing count did not run out of room for its answers. It ran
   out building `(ad+cb)/bd`, a product of two denominators that it was
   about to reduce away again.

4. Far enough out, the answer itself stops fitting
   2500 ballots, 35 candidates, 28 seats:

     cross-multiply  -> OVERFLOWED at seat 16 (widest so far 33 bits)
     lcm             -> OVERFLOWED at seat 27 (widest so far 73 bits)

   The better addition bought eleven seats, not safety. No fixed width
   covers an unbounded quantity, and the width this count needs is not
   knowable until it has read the ballots — which is why it returns an
   Outcome rather than a number.
```
<!-- /output -->

Section 1 is the whole mechanism in one number. The quota is 6 ballots; Allison has 8 units of weight at the top score; so each of those ballots keeps `1 - 6/8`, and a denominator of **4** enters the count. Nothing chose 4 from a fixed menu — it is the total weight of a group whose membership depends on how people voted. Seat 3's denominators would be built from seat 2's weights the same way, and so on down.

## The width you need is a property of the ballots

That would be a merely theoretical objection if the growth were predictable in practice. It is not, and section 2 is the demonstration: eight electorates, all of them 900 ballots, 18 candidates and 12 seats, differing only in how people voted. The widest denominator the exact count needs ranges from **27 bits to 39 bits** — one of those elections needs about four thousand times the range of another of exactly the same size.

That is the sentence that decides the design. You cannot look at an election's parameters and choose an integer type, because the parameters are not what the requirement depends on. Nor can you measure it once and hard-code the answer, for the same reason the [companion lesson's kata](../scaled_integers/README.md#practice) fails: a constant derived from one election is a constant that the next election is not obliged to respect.

So the count in this example does not return a number. It returns an outcome:

```rust
enum Outcome {
    /// Every seat filled exactly. Carries the widest denominator it needed.
    Exact { winners: Vec<usize>, peak_bits: u32 },
    /// The exact answer to some step did not fit. Carries the seat it died on.
    Overflowed { seat: usize, peak_bits: u32 },
}
```

Every arithmetic operation on the `Ratio` type returns `Option`, and a `None` anywhere propagates into `Overflowed`. This is the same discipline as every other [`Option`](../../17_Option_and_Result/partial_functions/README.md) in this library — the case with no answer gets a value rather than a lie — applied to the one place where a voting engine most wants to lie to you, because the alternative is a wrapped negative vote total nobody notices.

## The intermediate is what overflows, not the answer

Section 3 is the part I did not expect, and it is the practically useful half of the page.

The same election is counted twice, with two implementations of addition that are both *exactly correct* and differ only in the size of the number they build on the way to the same reduced answer:

```rust
// schoolbook: a/b + c/d = (ad + cb) / bd
Ratio::new(self.num * other.den + other.num * self.den, self.den * other.den)

// lcm: put both over lcm(b, d) first — never larger, usually far smaller
let l = (self.den / gcd(self.den, other.den)) * other.den;
Ratio::new(self.num * (l / self.den) + other.num * (l / other.den), l)
```

One of them counts the whole 20-seat election. The other gives up four seats short. And the widest denominator the finishing count ever had to *represent* was 68 bits — barely half of what `i128` holds. **The failing count never ran out of room for its answers; it ran out building a product it was about to reduce away again.**

Two things follow, and both are worth carrying past this page:

- **A fixed-width rational type fails much earlier than its answers require.** If you benchmark or bound such a type by the size of the values it stores, you will overestimate how far it goes, possibly by a factor of two in bits — which is to say by a factor of billions in range.
- **`checked_mul` returning `None` tells you an *operation* failed, not that the *problem* is too big.** The first response to an overflow in rational code should be to look at the route, not the type. Comparison is the other classic offender: `a/b < c/d` cross-multiplies too, and dividing the common factor out first (`a*(d/g)` vs `c*(b/g)`) moved the failure point in this example by several seats on its own.

## Where it gives out anyway, and what Rust hands you there

Section 4 is the honest end of the road: an election where both additions run out, the better one merely eleven seats later. No fixed width covers an unbounded quantity. At that point there are exactly three answers, and it is worth being blunt that all three are trades:

1. **Arbitrary-precision rationals.** Correct without qualification, unbounded in cost, and — this is the part worth knowing before you start — **Rust's standard library does not have them.** No `BigInt`, no `BigRational`, nothing. You reach for `num-bigint` and `num-rational`, which are excellent and are also a dependency, a compile-time cost, and a heap allocation per value. The language that made the companion lesson's fast path free charges you for the slow path that Python gives away, because Python's `int` was arbitrary-precision the whole time and its `Fraction` inherited that for nothing.
2. **Fixed-point with a stated rounding rule.** Pick a scale, write down which way you round, and the count can no longer overflow or fail. It also can no longer claim to be exact — but if the *rule* specifies the scale, then the rounded answer is the correct answer by definition rather than an approximation of one. This is not a dodge; it is what real election law does, and it is the [kata](#practice).
3. **Detect and escalate.** Run the cheap fixed-width count, and on `None` fall back to the expensive exact one. You get the common case fast and the hard case right, at the cost of maintaining two counts that must agree — and of a test suite that has to actually exercise the second one, which is the part that quietly does not happen.

What Rust contributes is not a way out of that trilemma. It is that the trilemma is *visible*: the type of the count is `Outcome`, so every caller has to say what it does when the arithmetic gives out, and there is no path where the program silently picks answer 4 and wraps.

## If you are coming from another language

- **Python** — you would never find this bug, because `fractions.Fraction` sits on arbitrary-precision `int` and simply grows. That is a real advantage and it is why the reference implementations of these methods are written in Python. The cost is the one the companion lesson measured: unbounded correctness is also unbounded work, silently, with no signal when a count starts doing thousand-bit arithmetic. Rust inverts the default — you get told, and then you have to decide. Neither default is right; knowing which one you are standing in is.
- **ABAP** — packed decimal is fixed-point with the rounding rule built into the type, so answer 2 above is the one you already reach for, and `TYPE p DECIMALS n` is that decision written into the data declaration. The thing that transfers less well is the ceiling: a `p` field tops out at 16 bytes, and a computation that outgrows it raises at runtime rather than being caught in review. So the failure mode is the same shape as this page's — an arithmetic requirement that depends on the data, discovered while running — and the fix is the same too: decide the precision deliberately and write it down, rather than discovering it from a short dump.

## Practice

**Build the count that always finishes.** The exact count on this page can give up partway through a real election, and no integer width fixes that in general. So build the fixed-point alternative: weights as integer counts of `1/scale`, one stated rounding rule applied everywhere, no division outside it.

Then answer the question that decides whether you would ship it — **how much precision does it actually need?** Run it at scales from `10^18` down to `10^2` against the exact count as ground truth, on every election where the exact count survives to *be* ground truth. Find the coarsest scale that still reproduces the exact winners.

Then write down, in one sentence, what your answer establishes and what it does not.

<details markdown="1">
<summary><strong>Solution</strong></summary>

<!-- source:compounding_weights_kata -->
*[`compounding_weights_kata.rs`](examples/compounding_weights_kata.rs) in full — pasted here by `tools/run_examples.py` from the file CI compiles and runs.*

```rust
//! Kata solution — the count that always finishes, and what that costs.
//!
//! The lesson's exact count can run out of `i128` partway through a real
//! election, and there is no width that fixes it in general. So the shippable
//! answer is a fixed-point count: pick a scale, state a rounding rule, and never
//! divide outside it. It cannot overflow and it always produces a committee.
//!
//! The question this program answers is how much precision that actually needs,
//! and the answer is uncomfortable in a useful way.

use std::cmp::Ordering;

const K: i128 = 5;

fn gcd(mut a: i128, mut b: i128) -> i128 {
    while b != 0 {
        let t = a % b;
        a = b;
        b = t;
    }
    a.abs()
}

// ------------------------------------------------------------ exact (baseline)

#[derive(Clone, Copy, PartialEq, Eq)]
struct Ratio {
    num: i128,
    den: i128,
}

impl Ratio {
    fn int(n: i128) -> Ratio {
        Ratio { num: n, den: 1 }
    }
    fn new(num: i128, den: i128) -> Option<Ratio> {
        if den == 0 {
            return None;
        }
        let g = gcd(num, den);
        let g = if g == 0 { 1 } else { g };
        let (mut num, mut den) = (num / g, den / g);
        if den < 0 {
            num = -num;
            den = -den;
        }
        Some(Ratio { num, den })
    }
    /// The lcm form throughout — the lesson showed the schoolbook one dies sooner.
    fn add(self, o: Ratio) -> Option<Ratio> {
        let l = (self.den / gcd(self.den, o.den)).checked_mul(o.den)?;
        Ratio::new(
            self.num
                .checked_mul(l / self.den)?
                .checked_add(o.num.checked_mul(l / o.den)?)?,
            l,
        )
    }
    fn sub(self, o: Ratio) -> Option<Ratio> {
        self.add(Ratio { num: -o.num, den: o.den })
    }
    fn mul(self, o: Ratio) -> Option<Ratio> {
        Ratio::new(self.num.checked_mul(o.num)?, self.den.checked_mul(o.den)?)
    }
    fn div(self, o: Ratio) -> Option<Ratio> {
        if o.num == 0 {
            return None;
        }
        Ratio::new(self.num.checked_mul(o.den)?, self.den.checked_mul(o.num)?)
    }
    fn cmp_checked(self, o: Ratio) -> Option<Ordering> {
        let g = gcd(self.den, o.den);
        Some(
            self.num
                .checked_mul(o.den / g)?
                .cmp(&o.num.checked_mul(self.den / g)?),
        )
    }
    fn is_zero(self) -> bool {
        self.num == 0
    }
}

/// `None` when the exact answer stopped fitting in `i128`.
fn exact(ballots: &[Vec<i128>], seats: usize) -> Option<Vec<usize>> {
    let (nb, nc) = (ballots.len(), ballots[0].len());
    let mut w = vec![Ratio::int(1); nb];
    let mut alive: Vec<usize> = (0..nc).collect();
    let mut winners = Vec::new();
    let hare = Ratio::new(nb as i128, seats as i128)?;

    for seat in 1..=seats {
        let mut best: Option<(Ratio, usize)> = None;
        for &c in &alive {
            let mut total = Ratio::int(0);
            for i in 0..nb {
                if ballots[i][c] != 0 && !w[i].is_zero() {
                    total = total.add(w[i].mul(Ratio::int(ballots[i][c]))?)?;
                }
            }
            let better = match best {
                None => true,
                Some((b, _)) => total.cmp_checked(b)? == Ordering::Greater,
            };
            if better {
                best = Some((total, c));
            }
        }
        let win = best?.1;
        winners.push(win);
        alive.retain(|&c| c != win);
        if seat == seats {
            break;
        }

        let mut quota = hare;
        let mut sup: Vec<usize> = (0..nb)
            .filter(|&i| ballots[i][win] != 0 && !w[i].is_zero())
            .collect();
        while !sup.is_empty() {
            let mut top = Ratio::int(0);
            for &i in &sup {
                let s = w[i].mul(Ratio::int(ballots[i][win]))?;
                if s.cmp_checked(top)? == Ordering::Greater {
                    top = s;
                }
            }
            let mut grp = Vec::new();
            for &i in &sup {
                if w[i].mul(Ratio::int(ballots[i][win]))? == top {
                    grp.push(i);
                }
            }
            let mut allocated = Ratio::int(0);
            for &i in &grp {
                allocated = allocated.add(w[i])?;
            }
            if allocated.cmp_checked(quota)? != Ordering::Greater {
                for &i in &grp {
                    w[i] = Ratio::int(0);
                }
                sup.retain(|i| !grp.contains(i));
                quota = quota.sub(allocated)?;
                if quota.is_zero() {
                    break;
                }
                continue;
            }
            let keep = Ratio::int(1).sub(quota.div(allocated)?)?;
            for &i in &grp {
                w[i] = w[i].mul(keep)?;
            }
            break;
        }
    }
    Some(winners)
}

// --------------------------------------------------------------- fixed point
//
// THE RULE, stated once and applied everywhere:
//
//   A weight is an integer count of 1/`scale` units. A full ballot is `scale`.
//   Every division truncates toward zero — so a reweighted ballot is never
//   credited with more weight than it is owed, and the count can only ever
//   under-spend a quota, never over-spend it.
//
// Nothing here can overflow for any scale where `scale * scale` fits, and
// nothing here can fail to produce a committee.

fn fixed(ballots: &[Vec<i128>], seats: usize, scale: i128) -> Vec<usize> {
    let (nb, nc) = (ballots.len(), ballots[0].len());
    let mut w = vec![scale; nb];
    let mut alive: Vec<usize> = (0..nc).collect();
    let mut winners = Vec::new();
    let hare = (nb as i128) * scale / (seats as i128);

    for seat in 1..=seats {
        let mut best = (i128::MIN, 0usize);
        for &c in &alive {
            let mut t: i128 = 0;
            for i in 0..nb {
                if ballots[i][c] != 0 && w[i] != 0 {
                    t += w[i] * ballots[i][c];
                }
            }
            if t > best.0 {
                best = (t, c);
            }
        }
        let win = best.1;
        winners.push(win);
        alive.retain(|&c| c != win);
        if seat == seats {
            break;
        }

        let mut quota = hare;
        let mut sup: Vec<usize> = (0..nb)
            .filter(|&i| ballots[i][win] != 0 && w[i] != 0)
            .collect();
        while !sup.is_empty() {
            let top = sup.iter().map(|&i| w[i] * ballots[i][win]).max().unwrap();
            let grp: Vec<usize> = sup
                .iter()
                .copied()
                .filter(|&i| w[i] * ballots[i][win] == top)
                .collect();
            let allocated: i128 = grp.iter().map(|&i| w[i]).sum();
            if allocated <= quota {
                for &i in &grp {
                    w[i] = 0;
                }
                sup.retain(|i| !grp.contains(i));
                quota -= allocated;
                if quota == 0 {
                    break;
                }
                continue;
            }
            let keep = scale - (quota * scale / allocated); // truncates
            for &i in &grp {
                w[i] = w[i] * keep / scale; // truncates
            }
            break;
        }
    }
    winners
}

// ------------------------------------------------------------------ ballots

struct Ballots(u64);
impl Ballots {
    fn next(&mut self, m: u64) -> u64 {
        self.0 = self
            .0
            .wrapping_mul(6364136223846793005)
            .wrapping_add(1442695040888963407);
        (self.0 >> 33) % m
    }
    fn election(seed: u64, voters: usize, candidates: usize) -> Vec<Vec<i128>> {
        let mut g = Ballots(seed);
        (0..voters)
            .map(|_| (0..candidates).map(|_| g.next(K as u64 + 1) as i128).collect())
            .collect()
    }
}

// --------------------------------------------------------------------- main

fn main() {
    const SCALES: [(&str, i128); 6] = [
        ("10^18", 1_000_000_000_000_000_000),
        ("10^12", 1_000_000_000_000),
        ("10^9", 1_000_000_000),
        ("10^6", 1_000_000),
        ("10^4", 10_000),
        ("10^2", 100),
    ];

    const RUNS: u64 = 6;
    for &(nb, nc, seats) in &[(700usize, 16usize, 10usize), (2000, 30, 20)] {
        println!("{nb} ballots, {nc} candidates, {seats} seats — {RUNS} electorates\n");

        // Ground truth, where there is any.
        let elections: Vec<Vec<Vec<i128>>> =
            (1..=RUNS).map(|s| Ballots::election(s, nb, nc)).collect();
        let truth: Vec<Option<Vec<usize>>> =
            elections.iter().map(|e| exact(e, seats)).collect();
        let checkable = truth.iter().filter(|t| t.is_some()).count();
        let overflowed = RUNS as usize - checkable;

        println!("     exact count finished on {checkable} of {RUNS}, ran out of i128 on {overflowed}");
        if overflowed > 0 {
            println!("     (the fixed-point count returned a committee on all {RUNS})");
        }
        println!();

        println!("     scale   reproduces the exact winner list");
        for &(name, sc) in &SCALES {
            let agree = elections
                .iter()
                .zip(&truth)
                .filter(|(_, t)| t.is_some())
                .filter(|(e, t)| fixed(e, seats, sc) == *t.as_ref().unwrap())
                .count();
            let note = if agree == checkable { "" } else { "   <-- diverges" };
            println!("     {name:<7} {agree} of {checkable}{note}");
        }
        println!();
    }

    println!("Four decimal places reproduce every exact answer anyone could check.");
    println!("Two do not. The exact count that ran out of i128 was working to 68-odd");
    println!("bits of denominator to reach a result that 10^4 also reaches.");
    println!();
    println!("What that establishes: on these electorates, at these sizes, the");
    println!("approximation and the exact answer agree. What it does not establish:");
    println!("anything at all about an election with a tied round, which is exactly");
    println!("where a truncated weight and an exact one part company, and exactly the");
    println!("case the companion lesson opened with.");
}
```
<!-- /source -->

<!-- output:compounding_weights_kata -->
*Verified output of [`compounding_weights_kata.rs`](examples/compounding_weights_kata.rs) — regenerated by `tools/run_examples.py`, never hand-typed.*

```text
700 ballots, 16 candidates, 10 seats — 6 electorates

     exact count finished on 6 of 6, ran out of i128 on 0

     scale   reproduces the exact winner list
     10^18   6 of 6
     10^12   6 of 6
     10^9    6 of 6
     10^6    6 of 6
     10^4    6 of 6
     10^2    4 of 6   <-- diverges

2000 ballots, 30 candidates, 20 seats — 6 electorates

     exact count finished on 5 of 6, ran out of i128 on 1
     (the fixed-point count returned a committee on all 6)

     scale   reproduces the exact winner list
     10^18   5 of 5
     10^12   5 of 5
     10^9    5 of 5
     10^6    5 of 5
     10^4    5 of 5
     10^2    1 of 5   <-- diverges

Four decimal places reproduce every exact answer anyone could check.
Two do not. The exact count that ran out of i128 was working to 68-odd
bits of denominator to reach a result that 10^4 also reaches.

What that establishes: on these electorates, at these sizes, the
approximation and the exact answer agree. What it does not establish:
anything at all about an election with a tied round, which is exactly
where a truncated weight and an exact one part company, and exactly the
case the companion lesson opened with.
```
<!-- /output -->

**Four decimal places is enough, and two is not.** The exact count was carrying 68-bit denominators — and running out of `i128` on one election in six — to reach a committee that `10^4` reaches as well, every time anyone could check.

That is a genuinely awkward result, and the awkwardness is the lesson. The case for exact arithmetic in the companion page was airtight: a tied round is a tied round, and `f64` broke one. But "exact" turned out to be reachable only on elections small enough not to overflow, while the approximation finished every time — so on the elections where the two disagree about whether an answer exists at all, the exact count is the one with nothing to say.

Note precisely what the table establishes: *on these electorates, at these sizes, the two agree.* It establishes nothing about an election with a tied round, which is exactly where a truncated weight and an exact one part company, and exactly the case the companion lesson opened with. An empirical agreement is not an error bound, and no number of agreeing elections becomes one.

Which is why the real answer is the one the third option in the list above only half describes: **stop treating the rounding as an approximation and make it the rule.** Real STV and proportional legislation specifies the number of decimal places for surplus transfers, precisely because exactness is not achievable and a published, reproducible rounding rule is. Once the statute says four decimal places, a count to four decimal places is not an approximation of the right answer — it *is* the right answer, and it is one that any two implementations can agree on exactly.

</details>
